# CLAUDE.md

This file is a **router**, not a rulebook.

The rules for working in this repository live in exactly one place:

## → [AGENTS.md](./AGENTS.md)

Read `AGENTS.md` before doing any work here. It covers the project goal, skill
permissions, modeling discipline, team collaboration, the GitHub proxy requirement,
directory responsibilities, and KFlow boundaries.

**Do not add rules to this file.** To change how agents work in this repository,
edit `AGENTS.md` — that way Claude Code, Codex, and every other agent read the
same instructions, and there is no risk of the two drifting apart.

*(The file is named `AGENTS.md` in uppercase. Some references call it `agents.md`;
on Windows they resolve to the same file, but the tracked name is `AGENTS.md` —
use that casing so nothing breaks on case-sensitive systems.)*
