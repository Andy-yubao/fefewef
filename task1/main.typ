// problem1_refined.typ
// 风格目标：参考 2022 B030 / B035 / B086 的国赛论文排版。
// 单文件可编译；无外部图片、无彩色框、无现代卡片式装饰。

#set page(
  paper: "a4",
  margin: (
    top: 2.45cm,
    bottom: 2.35cm,
    left: 3cm,
    right: 3cm,
  ),
  numbering: "1",
  number-align: center + bottom,
)

#set text(
  lang: "zh",
  font: ("Times New Roman", "SimSun"),
  size: 10.5pt,
  fill: black,
)

#set par(
  justify: true,
  first-line-indent:(amount: 2em, all:true),
  leading: 0.78em,
  spacing: 0.78em,
)

#set figure(
  gap: 0.45em,
)

#show figure: it => block(
  above: 0.75em,
  below: 0.85em,
  breakable: false,
)[
  #it
]
#show figure.caption: set text(
  font: "SimSun",
  size: 9pt,
)
#show figure: it => block(
  above: 0.35em,
  below: 0.45em,
  breakable:false,
)[
  #it
]

#set math.equation(numbering: "(1)")

// -------------------- 参考论文式层次 --------------------

#let major(title) = block(
  width: 100%,
  above: 1.35em,
  below: 1.00em,
  breakable: false,
)[
  #align(center)[
    #text(
      font: "SimHei",
      size: 14pt,
      weight: "bold",
    )[#title]
  ]
]

#let sec(title) = block(
  above: 1.15em,
  below: 0.72em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 12pt,
    weight: "bold",
  )[#title]
]

#let subsec(title) = block(
  above: 0.95em,
  below: 0.58em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 10.5pt,
    weight: "bold",
  )[#title]
]

#let small-title(title) = block(
  above: 0.78em,
  below: 0.42em,
  breakable: false,
)[
  #text(weight: "bold")[#title]
]

// 仿早年论文的“算法表”：黑白、上下横线，无填充、无圆角。
#let algorithm(title, body) = block(
  width: 100%,
  above: 0.90em,
  below: 0.95em,
)[
  #line(length: 100%, stroke: 0.8pt)
  #v(0.30em)
  #set par(first-line-indent: 0em, leading: 0.65em, spacing: 0.42em)
  #text(size: 9.5pt, weight: "bold")[#title]
  #v(0.25em)
  #line(length: 100%, stroke: 0.45pt)
  #v(0.35em)
  #text(size: 9.5pt)[#body]
  #v(0.30em)
  #line(length: 100%, stroke: 0.8pt)
]

// 三线表标题，保持与 B035 一类论文相近的朴素风格。
#let table-title(body) = block(
  above: 0.80em,
  below: 0.38em,
)[
  #align(center)[#text(size: 9.5pt)[#body]]
]

#major[五、问题一：多点交会定位区域直径及圆覆盖的判别]


问题一要求根据若干检测点的位置及其对同一干扰源测得的示向度，利用交会定位法确定干扰源的可能位置范围及圆覆盖问题。由于测向机的示向度存在 $plus.minus 1^"°"$ 的确定性误差，单次观测并不能给出一条精确的方向射线，而只能将干扰源限制在一个有限宽度的方向区域内。

据此，本文首先将每个检测点的示向误差转化为平面上的线性几何约束，并通过多点约束求交得到干扰源的交会定位区域；随后结合半径为 $1800 " m"$ 的目标圆域先验，对定位区域进行进一步限定。在此基础上求取定位区域内的最远点对，以其距离作为区域直径 $D$，最后由直径端点研究直径圆的圆心唯一性及其覆盖条件。

整个求解过程依次包括定位区域构造、区域直径计算与直径圆覆盖判别三个环节。



#sec[5.1.1 示向约束与定位区域]

多点交会定位的基础，是将各检测点获得的角度信息转化为关于干扰源坐标的可行域约束。下文将采用从单个检测点的示向建立约束，再将多个检测点的约束统一求交，最终分类讨论该交集区域与目标圆域的相对位置关系，以获得完整的定位区域信息。

#subsec[5.1.1 单检测点示向约束]

设第 $i$ 个检测点为 $S_i=(x_i,y_i)$，其测得示向度为 $hat(theta)_i$，待定位干扰源的位置记为 $X=(x,y)$。定义方向单位向量

$
u(theta) = (cos theta, sin theta).
$ <eq-direction>

由于测向机的示向度与真实方位之间存在误差，真实方位角并不固定为 $hat(theta)_i$，而应落在其两侧 $epsilon=1^"°"$ 的范围内，即


$
hat(theta)_i-epsilon <= theta_i <= hat(theta)_i+epsilon,
quad epsilon=1^"°".
$ <eq-angle-range>

因此，以检测点 $S_i$ 为顶点，干扰源的真实方向被限制在一个夹角为 $2epsilon$ 的狭窄角域中。记这一角域的两条边界方向分别为

$
theta_i^- = hat(theta)_i-epsilon,
quad
theta_i^+ = hat(theta)_i+epsilon.
$ <eq-boundary-angle>

本文采用二维向量叉积统一刻画候选方向相对两条边界射线的位置，以避免示向度在 $0^"°"$ 与 $360^"°"$ 附近需要额外的分段讨论。对二维向量 $a=(a_x,a_y)$、$b=(b_x,b_y)$，定义

$
a times b = a_x b_y-a_y b_x.
$ <eq-cross>

则候选位置 $X$ 与第 $i$ 次观测相容，当且仅当

$
u(theta_i^-) times (X-S_i) >= 0,
$ <eq-halfplane-1>

$
u(theta_i^+) times (X-S_i) <= 0.
$ <eq-halfplane-2>

@eq-halfplane-1 与 @eq-halfplane-2 共同给出了检测点 $S_i$ 对干扰源位置的限制，其公共部分记为 $W_i$。将叉积展开可见，上述两个不等式关于未知坐标 $(x,y)$ 均为一次不等式，因此一个带有 $plus.minus 1^"°"$ 示向误差的角度观测，可以等价地表示为两个闭半平面的交。

这样，每次带有误差的测向信息就被转化为两个统一的线性几何约束，便于后续多个检测点示向信息的交会计算。

#figure(
  image("figures/fig1_single_bearing_wedge.pdf", width: 66%),
  caption: [单检测点示向误差的几何约束（角度示意，非按比例）],
) <fig-single-wedge>

#subsec[5.1.2 多检测点约束求交]

单个检测点的示向信息将干扰源限制在一个方向区域内，而同一干扰源的真实位置则应与所有检测结果同时相容。设共有 $m$ 个检测点，第 $i$ 个检测点所给出的可行方向区域为 $W_i$，则全部测向信息共同确定的交会区域为


$
P = W_1 inter W_2 inter dots.c inter W_m.
$ <eq-intersection>

由于每个 $W_i$ 都是两个闭半平面的交，因此均为凸集；而凸集的交仍为凸集，所以 $P$ 同样具有凸性。当各检测点从不同方向对干扰源位置形成充分约束，且公共区域有界、非退化时，$P$ 即形成题目所述的凸多边形定位区域。

下面进一步说明如何由半平面约束得到该多边形的顶点。由于每个检测点提供两条边界直线，共有

$
h=2m
$

条半平面边界。若 $P$ 为有界凸多边形，则任意一个顶点都位于至少两条有效边界的交汇处，也就是说，在该顶点至少有两个半平面约束同时取等号。因此，$P$ 的全部顶点必然包含在这 $h$ 条边界直线的两两交点之中。

基于这一性质，首先枚举任意两条非平行边界直线并求得交点。对于任一候选交点 $Q$，再依次将其代入全部 $h$ 个半平面不等式。只有当

$
Q in W_i,
quad i=1,2,dots.c,m
$

同时成立时，该点才可能属于最终交会区域。由此删去违反任一示向约束的交点，仅保留满足全部约束的可行交点。最后对可行点去重并求凸包，按照逆时针方向排列，即得到交会区域 $P$ 的有序顶点。

从计算量来看，$h$ 条边界直线共形成 $O(h^2)$ 个候选交点，而每个候选点需要经过 $O(h)$ 个半平面约束的可行性检验，因此这一过程的最坏时间复杂度为

$
O(h^2) times O(h)=O(h^3).
$

问题一涉及的检测点数量较少，因此该算法的实际计算规模有限；同时，显式枚举候选交点还能清晰保留每个定位区域顶点的几何来源，便于后续核验。


#figure(
  image("figures/fig2_multi_station_intersection.pdf", width: 92%),
  caption: [多检测点示向约束交会与定位区域形状示意图],
) <fig-multi-intersection>

#subsec[5.1.3 目标圆域约束]

题目还给出了干扰源位置的范围：所有干扰源均位于以原点 $O$ 为圆心、半径 $R_0=1800 " m"$ 的目标圆域内。因此，将这一范围与前文的交会结果取交，最终定位区域应取为

$
Omega = P inter B(O,R_0).
$ <eq-omega>


其中，$B(O,R_0)$ 表示以 $O$ 为圆心、$R_0$ 为半径的闭圆域。随后检验凸多边形 $P$ 的各个顶点。若所有顶点均满足

$
norm(V_j) <= R_0,
$ <eq-inside-disk>

则由 $P$ 的凸性可知，整个多边形均包含在目标圆域内，即

$
P subset.eq B(O,R_0).
$

此时初始圆域的范围约束并未进一步缩小定位范围，因而有 $Omega=P$。
#v(0.9em)
#v(0.9em)



若存在顶点位于目标圆域之外，则圆周成为定位区域的一部分有效边界。此时进一步求取多边形各边与圆周之间的交点，并保留圆内的有效线段；此时，$partial Omega$ 可以精确表示为若干直线段与圆弧的组合。至此，由多点示向信息与目标区域先验共同确定的最终定位区域 $Omega$ 已构造完成。

#figure(
  image("figures/fig3_active_target_disk_clipping.pdf", width: 76%),
  caption: [与题设圆域范围有交集的情形],
) <fig-disk-clipping>

#sec[5.2 定位区域直径的求解]

在得到最终定位区域 $Omega$ 后，按照题目要求求解定位范围区域的直径，即范围区域内任意两点欧氏距离的最大值，

$
D = max_(X,Y in Omega) norm(X-Y).
$ <eq-diameter>

由于 $Omega$ 是闭、有界凸集，上式中的最大值一定存在，并且可以在区域边界上取得。

根据目标圆域是否参与最终边界，$Omega$ 可能是纯凸多边形，也可能具有由线段和圆弧共同组成的边界。下面分别处理这两种情形。


#subsec[5.2.1 凸多边形的情形]

若 $P subset.eq B(O,R_0)$，则 $Omega=P$ 为凸多边形。设其有序顶点为 $V_1,V_2,dots.c,V_n$。固定其中一个端点时，距离函数在凸多边形上的最大值必在某个顶点处取得，故两个直径端点均可从顶点集中选取：

$
D = max_(1 <= j < k <= n) norm(V_j-V_k).
$ <eq-vertex-diameter>

因此，直接枚举 $n(n-1)/2$ 个顶点对即可同步得到直径 $D$ 与一组直径端点 $A,B$，时间复杂度为 $O(n^2)$。由于交会定位区域通常只有少量顶点，直接枚举已经能够满足计算需求。

#subsec[5.2.2 边界中包含圆弧的情形]

若交会多边形 $P$ 有部分区域越出目标圆域，则最终定位区域的边界 $partial Omega$ 同时包含直线段和圆弧。此时式 @eq-vertex-diameter 所采用的端点枚举仍然有效，但仅比较离散端点不能排除最远点落在圆弧内部的可能性，因此需要进一步补充圆弧上的极值候选。

具体考虑以下三类候选点对：

① 全部线段端点与圆弧端点之间的点对；

② 对任一边界端点 $Q$，若其关于原点的对径点 $-R_0Q/norm(Q)$ 落在有效圆弧上，则将该点加入候选；

③ 若两段有效圆弧中存在极角相差 $pi$ 的一对点，则将该对径点对加入候选。

上述候选的选取来源于边界距离函数的极值性质。

对于一条线段，固定另一端点后，距离平方关于线段参数为凸函数，因此最大值只能出现在其端点；而对于圆弧，固定另一点后，距离函数在圆弧内部出现极值时，对应方向必与另一点形成反向对径关系。

因此，线段—线段情形可由端点枚举完全覆盖；线段—圆弧情形除了端点外，只需补充可能存在的反向对径点；圆弧—圆弧情形则还需检验两个有效圆弧之间可能形成的对径点对。由此，原本定义在连续边界上的直径问题仍然可以化为有限数量候选点对的距离比较。

#algorithm("算法 1　定位区域直径计算", [
  *输入：* 检测点坐标 $S_i$，示向度 $hat(theta)_i$，误差上界 $epsilon=1^"°"$，目标圆域半径 $R_0=1800 " m"$。

  *Step 1.* 根据式 @eq-halfplane-1、@eq-halfplane-2，将每个检测点的示向误差区间转化为两个闭半平面约束方程。

  *Step 2.* 枚举各半平面边界直线的两两交点，利用全部约束进行可行性筛选，并对保留的可行点求凸包，得到交会区域 $P$。

  *Step 3.* 引入目标圆域方程，计算 $Omega=P inter B(O,R_0)$。若 $P$ 全部位于圆域内，则取 $Omega=P$；否则解析保留裁剪后的有效线段与有效圆弧。

  *Step 4.* 若 $Omega$ 为纯多边形，则枚举全部顶点对；若边界含圆弧，则在端点候选之外补充反向点及圆弧对径点。

  *Step 5.* 比较全部候选点对之间的距离，将最大值记为 $D$，并记录一组达到该距离的端点 $A,B$。

  *输出：* 最终定位区域 $Omega$、区域直径 $D$ 及对应直径端点 $A,B$。
])
#sec[5.3 直径圆的覆盖性判别]

区域直径 $D$ 描述的是定位区域内部两点之间能够达到的最大距离，但这一量是否能够直接作为覆盖圆的直径，还需要进一步判断。下面证明：如果存在一个直径恰为 $D$ 的圆能够覆盖整个定位区域，那么该圆的圆心实际上已被一对直径端点唯一确定。

设 $A,B in Omega$ 为第 5.2 节求得的一对直径端点，则有

$
norm(A-B)=D.
$ <eq-ab-diameter>

假设存在一个直径为 $D$ 的圆能够覆盖整个定位区域，记其圆心为 $C$，半径为 $D/2$。由于 $A$ 与 $B$ 均属于定位区域，因此二者也必须同时包含在该圆中，即

$
norm(A-C) <= frac(D,2),
quad
norm(B-C) <= frac(D,2).
$

另一方面，由三角不等式，

$
D
= norm(A-B)
<= norm(A-C)+norm(C-B)
<= frac(D,2)+frac(D,2)
= D.
$ <eq-unique-circle>

不等式链的首尾相等，说明其中每一步都只能取等号。于是 $A,C,B$ 三点必须共线，并且

$
norm(A-C)=norm(B-C)=frac(D,2).
$ <eq-center>

因此，圆心 $C$ 只能位于线段 $A$、$B$ 的中点，即

$
C_D=frac(A+B,2),
$

而候选圆半径固定为

$
R_D=frac(D,2).
$

这表明，当圆的直径限定为定位区域直径 $D$ 时，候选覆盖圆事实上是唯一的，并不存在通过平移圆心改善覆盖效果的自由度。于是题目中的圆覆盖问题可转化为一个直接的边界检验问题：只需判断定位区域中距离 $C_D$ 最远的点是否仍位于半径 $R_D$ 以内。

若满足

$
max_(X in Omega) norm(X-C_D) <= R_D,
$

则该圆能够覆盖整个定位区域；反之，只要存在一个边界点到 $C_D$ 的距离大于 $R_D$，即可判定直径圆无法完成覆盖。

#algorithm("算法 2　直径圆覆盖判别", [
  *输入：* 定位区域边界 $partial Omega$、区域直径 $D$ 及一组直径端点 $A,B$。

  *Step 1.* 由直径端点唯一确定候选圆，令圆心 $C_D=(A+B)/2$，半径 $R_D=D/2$。

  *Step 2.* 计算所有线段端点和圆弧端点到 $C_D$ 的距离；若边界含有效圆弧，则同时检查距离函数在圆弧内部可能出现的驻点。

  *Step 3.* 若上述边界点到 $C_D$ 的最大距离不超过 $R_D$，则该直径圆能够覆盖 $Omega$；否则，以距离最大的边界点作为不能完全覆盖的直接证据。

  *输出：* 覆盖判定及最远边界点。
])


为验证前述交会定位、直径计算及覆盖判别方法，选取三组检测点及其对应示向度进行计算。具体输入数据如表 1 所示。

#table-title[表 1　六边形算例的检测点与示向度]

#table(
  columns: (18%, 27%, 27%, 28%),
  align: center,
  inset: (x: 4pt, y: 3pt),
  stroke: none,

  table.hline(stroke: 0.8pt),
  [*检测点*], [$x/"m"$], [$y/"m"$], [*示向度/$"°"$*],
  table.hline(stroke: 0.45pt),

  [$S_1$], [713.63], [1193.59], [235.05529],
  [$S_2$], [-939.50], [362.10], [356.78364],
  [$S_3$], [590.63], [-611.06], [117.85767],

  table.hline(stroke: 0.8pt),
)

按照第 5.1 节的方法，将三组观测分别转化为对应的半平面约束并进行求交，可得到一个六边形交会定位区域。进一步检验该区域与目标圆域的位置关系，发现六个顶点均满足式 @eq-inside-disk，因此半径 $1800 " m"$ 的目标圆域在本算例中并不是活跃约束，从而有

$
Omega=P.
$

最终得到的六个有序顶点列于表 2。

#table-title[表 2　交会定位区域的有序顶点]

#table(
  columns: (20%, 40%, 40%),
  align: center,
  inset: (x: 4pt, y: 3pt),
  stroke: none,

  table.hline(stroke: 0.8pt),
  [*顶点*], [$x/"m"$], [$y/"m"$],
  table.hline(stroke: 0.45pt),

  [$V_1$], [78.704085], [317.913679],
  [$V_2$], [96.440589], [285.727879],
  [$V_3$], [102.254593], [285.299256],
  [$V_4$], [121.919084], [314.513835],
  [$V_5$], [118.558048], [321.150947],
  [$V_6$], [82.075102], [322.562913],

  table.hline(stroke: 0.8pt),
)

由于本例中的 $Omega$ 为纯凸多边形，可直接依据式 @eq-vertex-diameter 枚举全部顶点对。计算得到一组最远顶点为 $V_1$ 与 $V_4$，因此取

$
A=V_1,
quad
B=V_4.
$

相应的定位区域直径为

$
D = norm(V_1-V_4)=43.348530 " m".
$ <eq-numeric-d>

根据第 5.3 节的结论，一旦直径端点 $A,B$ 确定，直径为 $D$ 的候选覆盖圆也随之唯一确定。其圆心为直径端点的中点，即

$
C_D = frac(V_1+V_4,2)
= (100.311585,316.213757).
$ <eq-cd>

而圆的半径为

$
R_D = frac(D,2)
= 21.674265 " m".
$ <eq-rd>

若该圆能够覆盖整个定位区域，则其余所有顶点到 $C_D$ 的距离均不应超过 $R_D$。然而，对顶点 $V_2$ 计算得到

$
norm(V_2-C_D)=30.730658 " m" > R_D,
$ <eq-v2>

而对顶点 $V_3$ 同样有

$
norm(V_3-C_D)=30.975501 " m" > R_D.
$ <eq-v3>

可见，$V_2$ 与 $V_3$ 均位于该直径圆之外。因此，即使圆的直径取为定位区域内任意两点间能够达到的最大距离，该圆仍无法覆盖整个六边形定位区域。相应的几何关系如图 @fig-counterexample 所示。

#figure(
  image("figures/fig4_hexagon_counterexample.pdf", width: 82%),
  caption: [直径圆不能覆盖定位区域的六边形反例],
) <fig-counterexample>

为了进一步说明这种现象，继续计算该六边形的最小覆盖圆，得到

$
R_("min")=23.098117 " m",
quad
2R_("min")=46.196234 " m" > D.
$ <eq-mec>
#v(0.9em)


此外，作为一般性说明，由平面 Jung 定理还可得到

$
R_("min") <= frac(D,sqrt(3)),
quad
2R_("min") <= frac(2D,sqrt(3)) approx 1.155D.
$ <eq-jung>

这意味着，对于一般平面凸集，区域直径 $D$ 只能保证存在一个直径不超过 $2D/sqrt(3)$ 的覆盖圆，却不能保证直径恰为 $D$ 的圆一定能够覆盖该区域。本算例由实际示向约束构造得到的六边形，正好给出了题目所问命题的直接反例。


#sec[5.5 结论]

问题一的核心是由带有误差的角度观测构造定位区域，并在此基础上研究该区域的最大空间跨度及其圆覆盖性质。本文首先将每个检测点的 $plus.minus 1^"°"$ 示向误差转化为两个线性半平面约束，通过多个检测点的约束求交获得凸交会区域 $P$；随后结合半径 $1800 " m"$ 的目标圆域范围限制，得到最终定位区域

$
Omega=P inter B(O,R_0).
$

在直径计算方面，

当目标圆域不参与边界时，$Omega$ 为纯凸多边形，其区域直径可以直接表示为

$
D = max_(1 <= j < k <= n) norm(V_j-V_k),
$

#v(0.9em)

当目标圆域参与边界时，则进一步补充有效圆弧上的反向点与对径点候选，从而仍可将连续边界上的最远点问题转化为有限候选点对之间的距离比较。

对于圆覆盖问题，若 $A,B$ 为一对直径端点，则任何直径为 $D$ 且同时包含 $A,B$ 的圆，其圆心都只能取为

$
C_D=frac(A+B,2),
quad
R_D=frac(D,2).
$

因此，是否能够覆盖整个定位区域只需检验这一唯一候选圆即可。算例中得到区域直径

$
D=43.348530 " m",
$

但存在顶点到候选圆心的距离大于 $D/2$；进一步计算得到最小覆盖圆直径为 $46.196234 " m"$，同样大于区域直径。

由此得到问题一的最终结论：*多点示向约束可以通过半平面求交构造定位区域，并通过边界最远点对计算其直径；但以定位区域直径为直径的圆并不能保证覆盖整个定位区域。*


