// main.typ —— 2026 高教社杯全国大学生数学建模竞赛 B 题完整论文初稿
// 无线电干扰源的快速自动定位与清除
//
// 约定：
//   * 所有数值均来自仓库内可复现产物，正文中标注来源文件。
//   * 尚无成图的位置使用 #figslot 生成"图片占位符 + 内容说明"。
//   * 允许暂时空缺之处：问题三的敏感性分析、参考文献（详见文末说明）。

#set page(
  paper: "a4",
  margin: (
    top: 2.45cm,
    bottom: 2.35cm,
    left: 3cm,
    right: 3cm,
  ),
  numbering: "1",
  number-align: center + bottom,
)

#set text(
  lang: "zh",
  font: ("Times New Roman", "SimSun"),
  size: 10.5pt,
  fill: black,
)

#set par(
  justify: true,
  first-line-indent: (amount: 2em, all: true),
  leading: 0.78em,
  spacing: 0.78em,
)

#set figure(gap: 0.45em)

#show figure: set block(
  above: 0.35em,
  below: 0.45em,
  breakable: false,
)

#show figure.caption: set text(
  font: "SimSun",
  size: 9pt,
)

#set math.equation(numbering: "(1)")

// -------------------- 标题格式 --------------------

#let major(title) = block(
  width: 100%,
  above: 1.35em,
  below: 1em,
  breakable: false,
)[
  #align(center)[
    #text(
      font: "SimHei",
      size: 14pt,
      weight: "bold",
    )[#title]
  ]
]

#let sec(title) = block(
  above: 1.15em,
  below: 0.72em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 12pt,
    weight: "bold",
  )[#title]
]

#let subsec(title) = block(
  above: 0.95em,
  below: 0.58em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 10.5pt,
    weight: "bold",
  )[#title]
]

#let small-title(title) = block(
  above: 0.78em,
  below: 0.42em,
  breakable: false,
)[
  #text(weight: "bold")[#title]
]

// 仿早年论文的"算法表"：黑白、上下横线，无填充、无圆角。
#let algorithm(title, body) = block(
  width: 100%,
  above: 0.90em,
  below: 0.95em,
)[
  #line(length: 100%, stroke: 0.8pt)
  #v(0.30em)
  #set par(first-line-indent: 0em, leading: 0.65em, spacing: 0.42em)
  #text(size: 9.5pt, weight: "bold")[#title]
  #v(0.25em)
  #line(length: 100%, stroke: 0.45pt)
  #v(0.35em)
  #text(size: 9.5pt)[#body]
  #v(0.30em)
  #line(length: 100%, stroke: 0.8pt)
]

// 三线表标题，保持与参考论文相近的朴素风格。
#let table-title(body) = block(
  above: 0.80em,
  below: 0.38em,
)[
  #align(center)[#text(size: 9.5pt)[#body]]
]

// 图片占位符：用于"希望有图但当前仓库尚无成图"的位置。
#let figslot(cap, name, desc) = figure(
  block(
    width: 100%,
    stroke: 0.6pt + luma(45%),
    inset: 9pt,
    radius: 2pt,
  )[
    #align(center)[
      #text(size: 10pt, weight: "bold")[【图片占位符】]
      #v(0.30em)
      #text(size: 9.5pt)[#name]
      #v(0.35em)
      #text(size: 9pt, fill: luma(25%))[#desc]
    ]
  ],
  caption: cap,
)

// ==================================================
// 摘要
// ==================================================

#align(center)[
  #text(font: "SimHei", size: 16pt, weight: "bold")[
    无线电干扰源的快速自动定位与清除
  ]
  #v(0.35em)
  #text(font: "SimHei", size: 11pt)[
    ——基于有界误差交会定位、确定性覆盖搜索与动态路线优化的机器狗清除策略
  ]
]

#v(0.5em)

#text(font: "SimHei", size: 12pt, weight: "bold")[摘　要]

本文针对半径为 $1800 " m"$ 的目标圆域，建立"*有界误差交会定位 → 观测点优化 → 确定性覆盖搜索 → 动态路线规划*"的四级递进模型，依次解决问题一至问题四。全文坚持一个原则：*只使用题面保证的硬边界，不把 $plus.minus 1^"°"$ 示向误差当作高斯噪声，也不用一次无信号结果直接否定一个频道。*

*针对问题一，*将每次示向测量转化为两个闭半平面，与半径 $1800 " m"$ 的目标圆域求交得到定位区域 $Omega$；当 $partial Omega$ 含圆弧时，利用"固定点对圆弧的距离仅在弧端点或反向点取极大""两条同圆圆弧的内部极值只有对径点"两条极值性质，把连续边界上的直径计算化为有限候选点对，无需离散采样。*核心命题*：若存在直径为 $D$ 的圆覆盖 $Omega$，则由三角不等式取等条件可证其圆心只能是直径端点 $A,B$ 的中点 $C_D$，即候选圆唯一。据此构造了一组*题目允许*的三站算例（三个读数对真实目标 $(100,300)$ 的包裹误差均落在 $plus.minus 1^"°"$ 内），得定位区域直径 $D=43.3485 " m"$、唯一候选直径圆半径 $D/2=21.6743 " m"$，而另两顶点到 $C_D$ 的距离分别为 $30.7307 " m"$ 与 $30.9755 " m"$，均已越出；该区域的最小覆盖圆半径 $R_("min")=23.0981 " m"$，对应直径 $46.1962 " m"$，比 $D$ 大 $6.57%$。*结论：以区域直径为直径的圆一般不能覆盖定位区域。*

*针对问题二，*先由第一次示向、目标圆域与 $1000$--$1500 " m"$ 有效接收半径确定可行域 $F_1$，并把第二检测点候选区域写为 $C_f = F_1 plus B(0,1500)$（$F_1$ 与半径 $1500 " m"$ 圆盘的 Minkowski 和），实际搜索域取 $C = Omega inter C_f$。在统一候选域、统一第一次观测和统一更新规则下比较四种选点准则，最终*采用平均 GDOP 最小化*，即在 $F_1$ 内取后验样本、最小化第二站位置的平均几何精度因子。在 $400$ 个基础状态 $times$ $25$ 次第二次观测共 $10000$ 个场景中，平均 GDOP 法的平均定位直径 *$52.38 " m"$*，低于几何法的 $67.67 " m"$、FIM E-最优法的 $193.41 " m"$ 与随机法的 $588.04 " m"$；配对 bootstrap 显示其相对几何法平均小 $15.29 " m"$（95% 区间 $[11.33,19.50]$），相对随机法小 $535.66 " m"$。

*针对问题三，*由"最小有效接收半径 $1000 " m"$"构造*七点覆盖证书*：原点与半径 $1200 " m"$ 正六边形六顶点构成的骨架，对目标圆域内任一点的*最坏距离为 $968.9016 " m"$，裕量 $31.0984 " m"$*，故七个点均无信号即可*证明*该频道不存在干扰源（而不是"猜它不存在"）。清除端用边长为 $5 " m"$ 的闭方格保守外包可行域，当包围圆半径 $r_c <= 19.75 " m"$ 时在圆心清除必然成功；长期未达标则退化为 $20 slash sqrt(2) = 14.142 " m"$ 的有限圆盘覆盖兜底。最终策略 `candidate_057_posterior_free` 在 $30$ 组配对验证中完成 $30/30$ 个案例、清除 $396/396$ 个干扰源，平均 $260.80 " s"/"个"$，相对确定性基线平均每例节省 $353.01 " s"$（胜率 $30/30$）；在 $100$ 例并发压力测试中清除 $1291/1291$ 个源，平均总时间 $3305.03 " s"$、每源 $256.01 " s"$。

*针对问题四，*定向源具有 $180^"°"$ 有效覆盖范围，一次无信号既可能是距离原因，也可能是检测点落在其覆盖方向之外，因此问题三的"无信号即排除"判据失效。本文改用*25 点双环发现覆盖*——外接正十二边形（半径 $1800/cos 15^"°" + 0.25 " m"$）与旋转 $15^"°"$ 的内环十二边形（半径 $980 " m"$）加原点构成的三角剖分，*每条剖分边均小于 $1000 " m"$*，故任一干扰源必落在某个三角形内、其三个顶点都在接收范围内，且任一过该源的闭半平面必含至少一个顶点，从而对 $180^"°"$ 定向源也给出发现证书。清除端用 $50 " m"$ 认证光学覆盖：先试圆心，失败后依次访问半径 $36 " m"$ 的十个等角环点，由凸性可证局部最坏距离 $19.2924 " m" < 20 " m"$。参数锁定后 $5$ 组共 $1000$ 个本地案例全部清除；其中混合场景平均任务时间由 $6641.99 " s"$ 降至 $6377.34 " s"$，固定 $16$ 源混合场景由 $6337.99 " s"$ 降至 $5742.93 " s"$。*敏感性分析*采用"既有数据分层 $+$ 冻结实例属性的 $3 times 5$ 全因子受控实验"两层证据（共 3000 次运行、1500 对配对，全部清除）：定向源占比 $p$ 是主导因素，其处理平方和占比在基线与候选策略下分别为 $85.73%$ 与 $66.61%$；$p: 0 arrow.r 1$ 使候选方案在 $N=10,13,16$ 下分别增时 $1098.02$、$1342.21$、$2434.10 " s"$，增量中约 $65%$--$74%$ 来自移动、$24%$--$30%$ 来自无线测量；而*源数效应非单调*——由于"发现全部 $16$ 个频道后可停止未见搜索"，$N=16$ 在高定向占比下反而可能快于 $N=10$，且 $N times p$ 交互显著（基线差中差 $1573.04 " s"$，区间 $[1420.76,1720.77]$）。因此本文明确区分"*可靠全清*"（$3000/3000$）与"*$6000 " s"$ 内完成*"（对 $N$ 与 $p$ 高度敏感），不宣称普遍达标。

本文的主要特点在于：把"清除的可靠性"与"路线的高效性"分离为两个互不干扰的层次——正确性由覆盖证书、硬可行集与最小包围圆证书保证，效率只允许改变访问顺序而不得删减任何保证层约束。*三个正式测试的结果按题面表 1 格式给出。*模型适用于把整片目标区域视为无地形障碍的平面、且设备误差为已知硬边界的场景；当接收半径分布、示向误差来源或源的空间分布发生改变时，需要按第八章的敏感性结论重新标定。

*关键词：* 无线电干扰源；交会定位；有界误差；最小包围圆证书；覆盖搜索；动态路线规划；定向干扰源；全因子敏感性分析

#pagebreak()

// ==================================================
// 一、问题重述
// ==================================================

#major[一、问题重述]

#sec[1.1 问题背景]

无线电干扰源的定位与清除是无线电频谱管理的重要任务，通常分初步监测、区域性排查和精准定位三个阶段。前两阶段可由固定监测站、移动监测车或无人机完成，但受环境遮挡与低功率源限制，*每个干扰源的最终精确定位仍需人员在目标区域内徒步排查*。某公司拟研发搭载于机器狗的自动定位清除系统以替代人工。

系统的检测原理为：测向机通过检测场强变化推断干扰源方位，当天线指向干扰源时接收场强最大，将该最大场强方向的方位角称为*示向度*。受环境与精度限制，示向度与真实方位角之间存在误差，且*在一段时间内同一地点的电磁环境干扰是固定的*，因此重复检测不会改变检测误差——只有当检测点改变、进入不同电磁环境时，误差才呈现统计规律。从目标区域全局看，所有误差落在 $[-1^"°",1^"°"]$ 内。

目标区域已确定为以原点为圆心、半径 $R_0 = 1800 " m"$ 的圆形区域，干扰源个数未知。设备与动作的物理参数见表 1-1。

#table-title[表 1-1　题目给定的物理参数与动作耗时]

#table(
  columns: (2.0fr, 1.1fr, 3.2fr),
  align: (left, center, left),
  stroke: 0.35pt,

  [参数], [取值], [说明],

  [示向度误差], [$plus.minus 1^"°"$], [硬边界，非统计分布],
  [有效接收半径], [$1000$--$1500 " m"$], [各源不同，测试中固定但未知],
  [目标圆域半径], [$1800 " m"$], [所有干扰源位于其内],
  [机器狗速度], [$5 " m"/"s"$], [沿直线行进],
  [检测耗时], [$5 " s"$], [含停下、调天线、读数稳定],
  [频道切换], [$1 " s"$], [任意两频道之间],
  [光学精确定位], [$3 " s"$], [仅在距源 $<= 20 " m"$ 时可用],
  [激光清除], [$2 " s"$], [光学定位成功后],
  [近距离直接清除], [$<= 5 " m"$], [信号过强，跳过示向直接清除],
)

#sec[1.2 问题提出]

#small-title[（1）问题一]

已知若干检测点坐标及同一干扰源关于这些检测点的示向度，要求给出采用交会定位法计算*多边形定位区域直径*（区域内任意两点距离的最大值）的算法；并回答：以该定位区域直径为直径的圆，能否覆盖此定位区域？

#small-title[（2）问题二]

已知某全向干扰源在一个检测点处测得的示向度，要求给出*第二个检测点的选择策略*，以获得关于该干扰源较好的定位效果，并给出第二个检测点的*候选区域*。

#small-title[（3）问题三]

目标区域中干扰源均为全向源，总数在 $10$--$16$ 之间但具体个数未知，各源频道互不相同（$1$--$20$ 中编号）。派一只机器狗从原点出发，测向机初始频道为 $1$，要求制定自动搜索、定位及清除策略并设计算法，在*确保所有干扰源被清除*的前提下使完成时间越短越好。需通过模拟器演练测试，给出"被清除干扰源个数的比例"与"平均定位清除时间"两个统计量；并完成三次正式测试，按题面表 1 格式填写测试案例编码、清除干扰源个数、平均定位清除时间与程序运行时间，导出日志放入支撑材料。

#small-title[（4）问题四]

目标区域中既有全向源又有定向源，总数仍为 $10$--$16$，具体个数未知，*定向源的个数及定向方向也未知*，其他条件与问题三相同。要求给出该条件下的检测与清除策略及算法，通过模拟器演练测试检验有效性，并同样完成三次正式测试。

#small-title[（5）题面提供的四个接口与计时规则]

模拟器只提供四条指令：`/enter`、`/exit`、`/measure`、`/clear`。移动与频道切换没有独立指令，由模拟器根据前后两次请求的位置与频道参数自动推断并计时。因此一次 `/measure` 的耗时由"移动 $+$ 切频 $+$ 检测 $5 " s"$"三部分组成；一次 `/clear` 由"移动 $+$ 光学 $3 " s"$（未发现）或 $3 + 2 = 5 " s"$（成功清除）"组成，且 `/clear` 的频道参数*不触发测向机切频*。检测返回三种结果：`direction`（附示向度）、`no_signal`、`near`（距离不超过 $5 " m"$ 且位于有效覆盖角内）。*这一"无信号有三种成因"的事实是问题四全部困难的根源。*

#pagebreak()

// ==================================================
// 二、问题分析
// ==================================================

#major[二、问题分析]

#sec[2.1 四个子问题的数学本质与依赖关系]

四个子问题并非彼此独立，而是构成一条"*几何基础 $arrow.r$ 观测设计 $arrow.r$ 单类型搜索 $arrow.r$ 混合类型搜索*"的递进链，见表 2-1。

#table-title[表 2-1　四个子问题的类型诊断与依赖关系]

#table(
  columns: (0.7fr, 1.6fr, 1.5fr, 2.4fr),
  align: (center, left, left, left),
  stroke: 0.35pt,

  [题号], [数学本质], [核心输出], [对前问的依赖],

  [问题一], [凸集（半平面交）几何度量], [定位区域 $Omega$ 与其直径 $D$], [—],
  [问题二], [有界误差下的观测点优化], [候选域 $C$ 与选点准则 $s^*$], [复用问题一的直径定义 $D(A)$],
  [问题三], [未知频道的确定性覆盖搜索], [七点骨架与清除证书], [复用问题二的选点思想],
  [问题四], [方向未知下的联合状态估计与搜索], [25 点双环与 $50 " m"$ 认证覆盖], [问题三的骨架与证书体系],
)

其中问题一与问题二解决"*给定观测如何确定位置*"，问题三与问题四解决"*未知全部信息时如何用有限步保证发现并清除*"。后两问的误差传播主要体现为：问题一、二中的定位直径越小，问题三、四中达到 $r_c <= 19.75 " m"$ 清除证书所需的观测次数越少，因而总时间越短。

#sec[2.2 问题一的分析]

示向度存在 $plus.minus 1^"°"$ 误差，所以每次检测给出的不是一条射线，而是一个*角度范围*。对任一待判位置 $X$，它与该次观测相容当且仅当"$X$ 相对检测点的方向"落在该角度范围内——这一条件可以写成*两个线性半平面*，与角度数值比较相比免除了 $0^"°"/360^"°"$ 跨界判断。多个检测点的约束取交即得交会区域 $P$，再与目标圆域取交得定位区域 $Omega$。

本问的两个技术难点在于：

1. *直径计算*。$Omega$ 的边界可能同时含线段与圆弧（当目标圆域真正发生裁剪时），此时"最远两点必为多边形顶点"的经验结论失效，若只在原顶点中枚举会系统性偏小。
2. *覆盖判别*。题目问的是"*以该直径为直径的圆*"能否覆盖，而不是"是否存在半径 $D/2$ 的覆盖圆"。这两个问题不同：前者的圆心被隐含地约束住了。必须先把圆心定下来，再判断覆盖。

对第 2 点，本文的切入点是三角不等式的取等条件，可以证明候选圆心*唯一*，从而使"能否覆盖"退化为一次确定性的最大距离检查（§5.3）。

#sec[2.3 问题二的分析]

第二次观测的价值来自两处矛盾的平衡：检测点离 $F_1$ 太远则收不到信号（受 $1500 " m"$ 最大接收半径限制），太近或与 $S_1$ 方向过于接近则两次视线的交会角过小、横向误差被放大。因此第二检测点的选择是一个*在"可接收"约束下最大化交会质量*的优化问题。

需要强调的是，本文不使用任何"期望直径"类的先验假设来替代硬约束。第二次检测结果有三种可能（`direction`、`no_signal`、`near`），对应的可行域更新规则完全不同，其中 `no_signal` 时*只能删除位于 $1000 " m"$ 必接收圆内的位置*（因为源的接收半径可能小到 $1000 " m"$），绝不能按 $1500 " m"$ 删除。

#sec[2.4 问题三的分析]

问题三的两个困难分别是"*不能遗漏*"和"*不能白走*"。

不能遗漏要求：即使某频道在若干次检测中都没有信号，也必须能*证明*它不存在，而不是推断它不存在。题面给出的$1000 " m"$ 最小接收半径提供了构造性工具——只要设计一组检测点，使目标圆域内*任意一点*到最近检测点的距离都不超过 $1000 " m"$，那么该点上的任何干扰源都必然在至少一个检测点被收到。这就把一个概率性的搜索问题变成了一个确定的覆盖几何问题。若七个点全部返回无信号，则该频道被*证明*不存在（§7.2）。

不能白走要求：在保证层之外，移动时间（$5 " m"/"s"$，占总时间约 $70%$ 以上）必须被压缩。这属于纯顺序优化，可以在*不触碰任何保证层约束*的前提下进行——这是本文把模型分为两层的直接原因。

#sec[2.5 问题四的分析]

加入定向源后，`no_signal` 的含义从两种（频道不存在 $slash$ 超出接收距离）变为三种，增加了"检测点落在定向方向两侧各 $90^"°"$ 之外的半平面"。此时问题三的核心判据——"七点全无信号即不存在"——*不再成立*，因为定向源的盲区可以覆盖全部七个检测点而源依然存在。

因此问题四需要替换的是*发现层的拓扑*，而不是调参。本文沿用"构造性覆盖证书"这一思想，但把骨架换成 25 点双环：其三角剖分的*每条边都短于最小接收半径 $1000 " m"$*，从而保证任一源所在的三角形三个顶点都能收到它；进一步，任一过该源的闭半平面（含 $180^"°"$ 的定向覆盖扇区）必然含三角形顶点之一，于是"发现"重新获得证书（§8.2）。

清除端则遇到新问题：定向源一旦进入盲区就再也收不到示向，只能用光学手段兜底。为此本文构造 $50 " m"$ 认证光学覆盖，把"在未知位置找到源"化为一个有限点的遍历（§8.3）。

#pagebreak()

// ==================================================
// 三、模型假设
// ==================================================

#major[三、模型假设]

本文每条假设都标注了它的*必要性*与*不成立时的影响*，不使用未被任何公式或算法调用的装饰性假设。

#table-title[表 3-1　模型假设及其依据与影响]

#table(
  columns: (0.45fr, 2.5fr, 1.7fr, 2.0fr),
  align: (center, left, left, left),
  stroke: 0.35pt,

  [编号], [假设内容], [依据], [不成立时的影响],

  [A1], [干扰源、检测点与机器狗视为同一水平面内的点，忽略高度差], [附录 2 第 7 条明确给出], [引入高度维，问题变为三维交会，几何结论需重写],
  [A2], [一次测试中源的位置、频道、接收半径、定向方向均不变], [题面"不随时间发生变化"], [需要动态滤波，本文的静态可行集方法失效],
  [A3], [示向度误差取硬边界 $[-1^"°",1^"°"]$，不视为高斯噪声], [题面给出范围而非分布；同地重复测量不产生独立误差], [若按高斯处理会给出无意义的置信区间，且违反"重复测量不缩小误差"的题设],
  [A4], [同一频道最多对应一个干扰源], [附录 1 第 1 条频道互不相同], [需要数据关联，频道不再等价于目标身份],
  [A5], [对全向源，检测点与源距离不超过 $1000 " m"$ 时必定收到信号], [$1000 " m"$ 是最小有效接收半径], [无信号不再能排除 $1000 " m"$ 内的区域，问题三的排除判据失效],
  [A6], [对定向源，A5 的结论不成立], [定向源在 $plus.minus 90^"°"$ 之外无辐射], [—（这是问题四必须处理的事实）],
  [A7], [机器狗只使用接口实际返回值决策，不使用模拟器隐藏的真值], [竞赛公平性与可复现性要求], [结果无法在正式测试中复现],
  [A8], [清除成功只以 `/clear` 返回 `success` 为准], [接口语义], [可能出现"自认为清除"的虚假完成],
)

#small-title[关于 A3 的补充说明]

题目明确指出"*在一段时间内，同一地点的电磁环境干扰是固定的，所以重复检测不会改变检测误差*"。这意味着在同一位置对同一频道反复检测*不能*获得独立样本、也不能缩小误差范围。因此本文全程采用集合式（硬约束）更新，而不使用任何需要误差独立同分布假设的统计推断。这是本文与"把 $plus.minus 1^"°"$ 当作标准差"的做法最本质的区别。

#pagebreak()

// ==================================================
// 四、符号说明
// ==================================================

#major[四、符号说明]

#table(
  columns: (1.5fr, 4.6fr, 1.3fr),
  inset: 5pt,
  align: (center, left, center),
  stroke: 0.5pt,

  [*符号*], [*含义*], [*单位/取值*],

  [$O$], [目标圆域圆心、坐标原点], [—],
  [$R_0$], [目标圆域半径], [$1800 " m"$],
  [$G$], [干扰源真实位置], [m],
  [$S_i$], [第 $i$ 个检测点 $S_i = (x_i,y_i)$], [m],
  [$hat(theta)_i$], [在 $S_i$ 处测得的示向度], [$"°"$],
  [$epsilon$], [示向度最大绝对误差], [$1^"°"$],
  [$W_i$], [第 $i$ 次示向度对应的角可行区域], [—],
  [$P$], [多个示向区域相交得到的交会区域], [—],
  [$Omega$], [最终定位区域 $P inter B(O,R_0)$], [—],
  [$D(A)$], [区域 $A$ 的直径 $max_(p,q in A) norm(p-q)$], [m],
  [$B(C,r)$], [以 $C$ 为圆心 $r$ 为半径的闭圆盘], [—],
  [$C_D$], [直径端点 $A,B$ 的中点，唯一候选直径圆圆心], [m],
  [$R_("min")$], [区域最小覆盖圆半径], [m],
  [$F_1$], [第一次示向后的可行域], [—],
  [$C_f, C_g$], [第二检测点的理论候选域与保证接收域], [—],
  [$C$], [第二检测点实际搜索域 $Omega inter C_f$], [—],
  [$J$], [方位观测的 Fisher 信息矩阵（线性化）], [—],
  [$G(S_2)$], [候选第二检测点的平均 GDOP], [—],
  [$A_7$], [问题三的七点搜索点集 ${O,V_1,dots.c,V_6}$], [—],
  [$A_25$], [问题四的 25 点双环搜索点集], [—],
  [$d_v, d_b$], [七点骨架的内 Voronoi 距离与边界中点距离], [m],
  [$F_c$], [频道 $c$ 的位置可行集（方格并集）], [—],
  [$"MEC"(P_c)$], [方格集 $P_c$ 的最小包围圆 $(m_c,r_c)$], [—],
  [$rho_c$], [安全清除证书阈值], [$19.75 " m"$],
  [$r_c^("opt")$], [问题四认证光学覆盖半径], [$50 " m"$],
  [$r_h$], [问题四环形清除点布置半径], [$36 " m"$],
  [$p$], [受控实验中定向源的生成概率], [—],
  [$N$], [案例中的干扰源个数], [$10$--$16$],
  [$T$], [机器狗执行任务的虚拟时间], [s],
)

#pagebreak()

// ==================================================
// 五、问题一
// ==================================================

#major[五、问题一：多点交会定位区域的直径与圆覆盖判别]

#sec[5.1 示向误差与定位区域的构造]

#subsec[5.1.1 单个检测点对应的定位范围]

设第 $i$ 个检测点为 $S_i=(x_i,y_i)$，测得的示向度为 $hat(theta)_i$，误差上界 $epsilon = 1^"°"$，则干扰源真实方位角应满足

$
theta_i^- = hat(theta)_i - epsilon, quad theta_i^+ = hat(theta)_i + epsilon .
$ <eq-angle>

记方向角 $theta$ 对应的单位向量为 $u(theta) = (cos theta, sin theta)$，二维向量的叉积为

$
a times b = a_x b_y - a_y b_x .
$ <eq-cross>

则对任意待判位置 $X=(x,y)$，向量 $X - S_i$ 表示从检测点指向该位置的方向。$X$ 与第 $i$ 个检测点给出的定位范围相容，当且仅当

$
u(theta_i^-) times (X - S_i) >= 0, quad u(theta_i^+) times (X - S_i) <= 0 .
$ <eq-halfplanes>

式 @eq-halfplanes 刻画了两个*闭半平面*，其交集记为 $W_i$，几何上是以 $S_i$ 为顶点、夹角 $2 epsilon$ 的扇形区域。

#figure(
  image("../paper_assets/paper_figures/section5/fig1_single_bearing_wedge.pdf", width: 56%),
  caption: [单个检测点的示向误差约束。以检测点为顶点的 $2 epsilon$ 扇形即为该次观测的全部相容位置。],
) <fig-single-wedge>

*说明*：不直接比较角度数值，而用 $sin,cos$ 与叉积表达约束，是本文实现中的一个关键选择。它使 $0^"°"$ 与 $360^"°"$ 的跨界情形无需任何特判，同时也避免了浮点角度比较在临界点附近的数值不稳定。

#subsec[5.1.2 多个检测点的交会区域]

设共有 $m$ 个检测点，各可行区域取交得到交会区域

$
P = W_1 inter W_2 inter dots.c inter W_m .
$ <eq-intersection>

由于每个 $W_i$ 都是凸集，$P$ 仍为凸集。求解时，设式 @eq-halfplanes 给出的半平面边界总数为 $h = 2m$：先枚举任意两条非平行边界直线的交点，再检查该交点是否满足全部 $h$ 个半平面约束；保留全部可行交点后求凸包，即得 $P$ 的有序顶点。候选交点有 $O(h^2)$ 个，每个需检查 $O(h)$ 个约束，故最坏时间复杂度为

$
O(h^3) .
$ <eq-complexity>

需要指出，该复杂度只针对本题观测站数量很少的情形，*不应被理解为通用半平面交算法的效率承诺*。若法向量无法围成有界区域（例如所有观测近乎平行），程序会显式报错而非给出错误区域。

#figure(
  image("../paper_assets/paper_figures/section5/section5_fig2_multi_station_intersection.pdf", width: 82%),
  caption: [多检测点示向约束的交会区域。左：三个检测点与目标圆域；右：局部放大，所得凸六边形即为交会区域 $P$。],
) <fig-multi-intersection>

#subsec[5.1.3 干扰源自身的位置范围与目标圆域裁剪]

所有干扰源均位于 $B(O,R_0)$ 内，故最终定位区域为

$
Omega = P inter B(O,R_0) .
$ <eq-omega>

利用*欧氏范数是凸函数*这一性质：凸多边形上范数的最大值必在顶点取得。因此若 $P$ 的全部顶点都满足 $norm(v) <= R_0$，则整个 $P$ 都在圆内，此时 $Omega = P$，目标圆域不生效。只有存在越界顶点时才需执行裁剪。

裁剪过程*不把圆近似为多边形*：对每条边解析求出线段落在圆内的参数区间；解析求出边与圆的交点并按极角排序；相邻交点之间的圆弧段若中点满足全部半平面约束则予保留。最终边界被精确表示为若干线段与若干圆弧的有序并集，并覆盖"圆盘完全含于 $P$"的退化情形。

#figure(
  image("../paper_assets/paper_figures/section5/section5_fig3_active_target_disk_clipping.pdf", width: 66%),
  caption: [目标圆域对交会区域的裁剪。示意算例中圆域半径取 $1000 " m"$ 以使圆弧清晰可见；题面实际取值 $R_0 = 1800 " m"$。],
) <fig-disk-clipping>

#subsec[5.1.4 区域直径的求解]

定义定位区域 $Omega$ 的直径为

$
D = max_(X,Y in Omega) norm(X - Y) .
$ <eq-diameter>

$Omega$ 为有界凸集，其直径必在边界上取得，故只需按边界的几何形式分类处理。

*(i) 边界为纯多边形时*，凸多边形中最远的两点必为两个顶点，故设顶点依次为 $V_1,dots.c,V_n$，只需比较全部顶点对距离

$
D = max_(1 <= j < k <= n) norm(V_j - V_k) .
$ <eq-polygon-diameter>

*(ii) 边界含圆弧时*，只比较原顶点会系统性低估，需补充圆弧内部的极值点。依据两条初等性质：

- 固定圆弧外一点 $p$，到圆周上点的距离只可能在*弧端点*或*反向点* $-R_0 p slash norm(p)$（若该点落在有效弧上）达到极大；
- 固定一条线段时，距离的平方对该线段参数是凸函数，故最大值必在端点取得；
- 两条同圆圆弧之间的内部极值，只可能是*对径点*（极角相差 $pi$）。

因此连续边界上的直径计算可化为*有限个候选点对*的比较，无需对圆弧离散采样，也不引入离散化误差。

#algorithm("算法 1　定位区域直径计算与覆盖判别", [
  *输入：* 检测点 $S_i$、示向度 $hat(theta)_i$、误差上界 $epsilon$、目标圆域半径 $R_0$。\
  *Step 1.* 按式 @eq-halfplanes 将每次示向范围转化为两个闭半平面。\
  *Step 2.* 枚举半平面边界交点、做可行性筛选、求凸包，得到交会区域 $P$。\
  *Step 3.* 计算 $Omega = P inter B(O,R_0)$；若圆域发生截断，则求出有效线段、圆弧及其端点。\
  *Step 4.* 对"线段--线段""线段--圆弧""圆弧--圆弧"三类边界组合分别计算可能的最大距离；圆弧情形补充反向极值点与可行对径点。\
  *Step 5.* 比较全部候选距离，取最大值作为区域直径 $D$，并记录一组直径端点 $A,B$。\
  *Step 6.* 令 $C_D = (A+B)/2$，检查是否有 $max_(X in Omega) norm(X - C_D) <= D/2$；若是则直径圆可覆盖 $Omega$，否则不可。
])

#sec[5.2 以区域直径为直径的圆的覆盖性问题]

#subsec[5.2.1 候选圆心的唯一性]

这是本问的关键一步。设 $A,B in Omega$ 为直径端点，$norm(A-B) = D$。若存在直径为 $D$ 的圆能同时覆盖 $A$ 与 $B$，设其圆心为 $C$，则由三角不等式

$
D = norm(A-B) <= norm(A-C) + norm(C-B) <= D/2 + D/2 = D .
$ <eq-triangle>

两端相等，故两个不等号同时取等，即 $A,C,B$ 共线且 $norm(A-C) = norm(C-B) = D/2$，从而

$
C_D = (A+B)/2 .
$ <eq-center>

*结论*：这个候选圆是*唯一*的——圆心只能是直径端点连线的中点，不能通过平移圆心获得其他候选。因此"是否存在直径为 $D$ 的覆盖圆"等价于"以 $A B$ 为直径的这一个圆是否覆盖 $Omega$"。

#subsec[5.2.2 覆盖判别准则]

由上文，直径圆能覆盖 $Omega$ 当且仅当

$
max_(X in Omega) norm(X - C_D) <= D/2 .
$ <eq-cover>

由于 $Omega$ 为凸集，该最大值在边界 $partial Omega$ 上取得：边界为线段时检查端点；边界含圆弧时检查弧端点以及圆弧上距 $C_D$ 最远的反向点。

#subsec[5.2.3 反例：一个题目允许的三站算例]

本文构造了一个*完全合法*的三站算例。取三个检测点与相应读数，使三个读数对真实目标 $(100,300)$ 的包裹误差均落在 $[-1^"°",1^"°"]$ 内，且每个扇区跨度恰为 $2^"°"$——即它是题目允许的真实测量结果，而非人为构造的违规几何。该算例的定位区域为凸六边形，目标圆域不生效。

对该六边形执行算法 1，得到：

#table-title[表 5-1　三站算例的直径圆覆盖检验]

#table(
  columns: (2.6fr, 1.6fr, 2.4fr),
  align: (left, center, left),
  stroke: 0.35pt,

  [量], [数值], [来源],

  [区域直径 $D$], [$43.3485 " m"$], [顶点 $V_1,V_4$ 达到],
  [直径圆圆心 $C_D$], [$(100.3116,\ 316.2138) " m"$], [式 @eq-center],
  [直径圆半径 $D/2$], [$21.6743 " m"$], [—],
  [$norm(C_D - V_2)$], [$30.7307 " m"$], [大于 $D/2$，越界],
  [$norm(C_D - V_3)$], [$30.9755 " m"$], [大于 $D/2$，越界],
  [最小覆盖圆圆心], [$(99.6854,\ 308.2540) " m"$], [独立数值复核],
  [最小覆盖圆半径 $R_("min")$], [$23.0981 " m"$], [支撑顶点为 $V_1,V_3,V_4$],
  [最小覆盖圆直径 $2R_("min")$], [$46.1962 " m"$], [比 $D$ 大约 $6.57%$],
)

#figure(
  image("../paper_assets/paper_figures/section5/fig4_hexagon_counterexample.pdf", width: 72%),
  caption: [六边形定位区域的直径圆覆盖反例。红色虚线为唯一候选直径圆，蓝色实线为最小覆盖圆；顶点 $V_2,V_3$ 落在直径圆之外。],
) <fig-counterexample>

可见六边形仍有顶点位于直径圆之外，即

$
max_(X in Omega) norm(X - C_D) > D/2 .
$ <eq-fail>

结合 §5.2.1 的候选圆唯一性，可进一步断定：*不存在任何以 $D$ 为直径且能覆盖该定位区域的圆*。"区域直径为 $D$"与"存在直径为 $D$ 的覆盖圆"是两个不同的命题。

#subsec[5.2.4 与最小覆盖圆的关系及 Jung 定理]

若允许重新选择圆心并增大半径，记能覆盖 $Omega$ 的最小圆半径为 $R_("min")$。由于任何覆盖圆都必须同时包含直径端点 $A,B$，故

$
R_("min") >= D/2 .
$ <eq-rmin-lower>

另一方面，平面 Jung 定理给出任意直径为 $D$ 的有界集合都有

$
R_("min") <= D/sqrt(3) .
$ <eq-jung>

合并得

$
D <= 2R_("min") <= 2D/sqrt(3) approx 1.155 D .
$ <eq-jung-range>

需要强调证据边界：$2/sqrt(3) > 1$，故*Jung 定理本身并不能推出"存在直径为 $D$ 的覆盖圆"*，它只给出了仅由区域直径可得的覆盖上界。本问的否定结论由 §5.2.1 的唯一性论证与表 5-1 的数值共同支持，Jung 定理只作为理论参照。正三角形可以取到 Jung 上界，这说明 $D/2$ 与真实最小覆盖半径的差距有明确的理论上限。

#sec[5.3 问题一结论]

每个检测点的示向度及其 $plus.minus 1^"°"$ 误差可转化为两个半平面约束；全部约束取交并与半径 $1800 " m"$ 的目标圆域求交，即得定位区域 $Omega$；依据边界中线段与圆弧的极值性质，可精确计算区域直径 $D$。对于任意一组直径端点 $A,B$，若存在直径为 $D$ 的覆盖圆，其圆心只能是 $C_D = (A+B)/2$。

*最终回答：多点交会可以严格求得定位区域及其直径，但以定位区域直径为直径的圆一般不能覆盖整个定位区域。*在本文给出的合法三站算例中，$D = 43.3485 " m"$ 而所需的 $2R_("min") = 46.1962 " m"$，直径圆无法覆盖。

#pagebreak()

// ==================================================
// 六、问题二
// ==================================================

#major[六、问题二：第二检测点的选择与定位区域优化]

#sec[6.1 第一次示向约束与可行域]

设第一次检测点 $S_1$ 与读数 $theta_1$。定义两个方位角 $alpha,beta$ 的最小角度差

$
delta(alpha,beta) = min( abs(alpha - beta), 360^"°" - abs(alpha - beta) ) .
$ <eq-angdiff>

记 $beta(S,g)$ 为从 $S$ 指向 $g$ 的方位角，则与一次观测 $(S,theta)$ 相容的位置集合为

$
W(S,theta) = { g : 5 < norm(g - S) <= 1500, delta(beta(S,g), theta) <= 1^"°" } .
$ <eq-w>

其中距离下界 $5 " m"$ 源于"距离不超过 $5 " m"$ 时信号过强、无法获得示向度"，上界 $1500 " m"$ 为题目给出的最大有效接收半径。与目标圆域求交得第一次可行域

$
F_1 = Omega inter W(S_1, theta_1) .
$ <eq-f1>

#sec[6.2 第二检测点的候选区域]

第二站必须能收到信号。由于最大有效接收半径为 $1500 " m"$，若某点到 $F_1$ 中*所有*位置的距离都超过 $1500 " m"$，则该点第二次检测必然收不到信号。因此理论候选区域为

$
C_f = { s : min_(g in F_1) norm(s - g) <= 1500 } = F_1 plus B(0,1500) ,
$ <eq-cf>

即 $F_1$ 与半径 $1500 " m"$ 圆盘的 *Minkowski 和*。考虑机器狗仍在目标圆域内选点，实际搜索域取

$
C = Omega inter C_f .
$ <eq-candidate>

此外还可定义保证接收域

$
C_g = { s : max_(g in F_1) norm(s - g) <= 1000 } .
$ <eq-cg>

因所有源的有效接收半径均不小于 $1000 " m"$，若 $s in C_g$ 则无论源实际位于 $F_1$ 何处，第二次检测必能收到信号。但 $C_g$ 通常偏小，可能排除交会角更好的位置，故*只用于说明而不用于限制选点*。

#small-title[候选区域的形状澄清]

由 $F_1 = Omega inter W(S_1,theta_1)$ 可知 $F_1$ 是一个*扇形环*与目标圆盘的交（内半径 $5 " m"$、外半径 $1500 " m"$、角半宽 $1^"°"$）。经 Minkowski 和后，$C_f$ 是一个*圆角鼓形区域*，既不是圆环也没有角度限制——第二次观测的方向是任意的。实测中 $C$ 几乎覆盖整个 $1800 " m"$ 目标圆盘，只有远离 $F_1$ 的一小片被切掉。

#figure(
  image("../paper_assets/paper_figures/section6/section6_fig1_candidate_regions.png", width: 80%),
  caption: [第一次定位区域与第二检测点候选区域。黑圈为目标圆域 $Omega$，浅蓝为理论候选域 $C_f$，绿色为保证接收域 $C_g$，橙色窄扇环为 $F_1$，蓝点为候选检测点，红星为按平均 GDOP 准则选出的 $S_2$，黑三角为 $S_1$。],
) <fig-q2-regions>

#sec[6.3 第二次观测后的可行域更新]

设第二检测点为 $s$，第二次检测有三种可能结果，对应的更新规则为

$
F_2 = cases(
  F_1 inter W(s, theta_2) & "获得示向度" ("direction"),\
  F_1 without B(s, 1000) & "无信号" ("no_signal"),\
  {G} & "距离不超过" 5 " m" ("near"),
) .
$ <eq-update>

*`no_signal` 一项是本题最容易被做错的地方*：源的接收半径未知且可能低至 $1000 " m"$，故无信号时*只能*删除位于 $1000 " m"$ 必接收圆内的位置，绝不能按 $1500 " m"$ 删除。若按 $1500 " m"$ 删除，会删掉真实位置，导致后续全部推断失效。`near` 情形下距离不超过 $5 " m"$，可直接光学定位，定位直径记为 $0$。

#sec[6.4 选点准则：平均 GDOP 最小化]

设候选点 $s$，在 $F_1$ 内按面积均匀抽取 $N_G$ 个可能的源位置 $g_j$，对每个 $g_j$ 计算两次视线的交会几何。记两台站到 $g_j$ 的距离分别为 $d_(1j) = norm(g_j - S_1)$ 与 $d_(2j)(s) = norm(g_j - s)$，视线夹角为 $alpha_j(s)$。方位观测的线性化 Fisher 信息矩阵为

$
J_j(s) = frac(1,sigma^2) sum_(i in {1,2}) frac(1,d_(i j)^2) bold(n)_(i j) bold(n)_(i j)^top ,
$ <eq-fim>

其中 $bold(n)_(i j)$ 为第 $j$ 个候选源位置相对第 $i$ 个站点的横向单位向量，$sigma = epsilon / sqrt(3)$ 由 $[-1^"°",1^"°"]$ 上的均匀分布折算得到。几何精度因子定义为

$
"GDOP"_j(s) = sqrt( "tr"( J_j(s)^(-1) ) ) .
$ <eq-gdop>

*选点准则*为最小化候选源位置上的平均 GDOP：

$
s^* = op("argmin")_(s in C) G(s), quad G(s) = frac(1,N_G) sum_(j=1)^(N_G) "GDOP"_j(s) .
$ <eq-gdop-opt>

选择平均 GDOP 而非其他准则的理由是：它在*有限接收约束*下同时刻画了"交会角接近直角"与"两站距离不过远"这两件事，并且计算成本很低（每候选点一次 $2 times 2$ 矩阵求逆），适合作为后续多源搜索中的快速选点内核。与此同时，GDOP 只是*选点*的启发式打分，*不参与可行域的更新*——可行域始终按式 @eq-update 的硬约束更新。

#sec[6.5 对照策略]

为检验该准则的效果，本文在同一候选域 $C$ 内、同一第一次观测下比较四种选点策略：

#table-title[表 6-1　四种第二检测点选择策略]

#table(
  columns: (1.5fr, 3.4fr, 1.5fr),
  align: (left, left, center),
  stroke: 0.35pt,

  [策略], [选点依据], [方向],

  [平均 GDOP], [$min_s E[ sqrt("tr"(J^(-1))) ]$，即式 @eq-gdop-opt], [最小化],
  [几何选点], [$max_s E[ bold(1)_({norm(G-s) <= R}) abs(sin alpha) / sqrt(d_1 d_2) ]$，接收加权的交会质量], [最大化],
  [FIM E-最优], [$max_s E[ lambda_("min")(J) ]$，信息矩阵最小特征值], [最大化],
  [随机选点], [在 $C$ 内均匀随机], [—],
)

#sec[6.6 算法]

#algorithm("算法 2　第二检测点选择", [
  *输入：* 第一次检测点 $S_1$、示向度 $theta_1$、目标圆域 $Omega$、有效接收半径上下界 $R_("min"), R_("max")$。\
  *Step 1.* 按式 @eq-w、@eq-f1 构造第一次可行域 $F_1$。\
  *Step 2.* 按式 @eq-cf、@eq-candidate 构造候选区域 $C = Omega inter C_f$，并以 $250 " m"$ 步长生成网格候选点。\
  *Step 3.* 在 $F_1$ 内按面积均匀抽取 $N_G = 80$ 个源位置样本，按式 @eq-fim、@eq-gdop 计算每个候选点的平均 GDOP。\
  *Step 4.* 对得分最优的若干候选点，用 $100 " m"$ 与 $50 " m"$ 网格逐级细化，输出 $s^*$。\
  *Step 5.* 实施第二次检测后，按式 @eq-update 用硬约束更新可行域 $F_2$，并计算 $D(F_2)$ 作为最终评价指标。
])

候选域的精细网格规模：$250 " m"$ 步长下 $x,y$ 各取 $-1800$ 至 $1800$ 共 $15 times 15 = 225$ 个网格点，其中落在目标圆盘内的 $162$ 个，经"至少存在一个后验样本位于 $1500 " m"$ 内"的可行性筛选后约剩 $89$ 个参与打分。

#sec[6.7 实验结果]

实验设计：生成 $400$ 个基础状态，每个状态进行 $25$ 次第二次观测（含不同的误差实现），共 $10000$ 个场景，随机种子 $20260911$。四种策略使用*完全相同*的第一次观测、候选域、后验样本与更新规则，唯一差异是 $S_2$ 的选择。

#table-title[表 6-2　四种第二检测点选择策略的定位区域直径（单位：m）]

#table(
  columns: (1.5fr, 1fr, 1fr, 1fr, 1fr, 1fr),
  align: center,
  stroke: 0.35pt,

  [策略], [均值], [中位数], [P90], [P95], [最大值],

  [平均 GDOP], [*52.38*], [*47.54*], [*71.66*], [*81.12*], [*306.71*],
  [几何选点], [67.67], [56.27], [120.21], [142.46], [453.83],
  [FIM E-最优], [193.41], [66.42], [609.44], [817.15], [1352.20],
  [随机选点], [588.04], [278.29], [1495.00], [1495.00], [1495.00],
)

#table-title[表 6-3　平均 GDOP 相对其他策略的配对改善（cluster bootstrap，2000 次重采样）]

#table(
  columns: (1.6fr, 1.3fr, 1.9fr, 1.6fr),
  align: center,
  stroke: 0.35pt,

  [对照策略], [平均改善/m], [95% 区间/m], [是否显著],

  [几何选点], [15.29], [[11.33, 19.50]], [是],
  [FIM E-最优], [141.03], [[115.03, 166.67]], [是],
  [随机选点], [535.66], [[482.16, 592.20]], [是],
)

#figure(
  image("../paper_assets/paper_figures/section6/section6_fig3_diameter_cdf.png", width: 76%),
  caption: [四种策略定位区域直径的经验累积分布。竖虚线为无有效二次测向时的退化值 $1495.003 " m"$（此时保留第一阶段区域直径）。],
) <fig-q2-cdf>

#figure(
  image("../paper_assets/paper_figures/section6/section6_fig2_selected_points.png", width: 78%),
  caption: [四种策略在同一可行域上选出的第二检测点。],
) <fig-q2-points>

#sec[6.8 结果分析]

由表 6-2 可见，平均 GDOP 法的平均定位直径为 $52.38 " m"$，中位数 $47.54 " m"$，是四种策略中平均值最小者；FIM E-最优法虽然中位数尚可（$66.42 " m"$），但 P90 高达 $609.44 " m"$，说明其在近平行几何下会出现严重的定位尾部；随机选点法有 $45.00%$ 的场景完全收不到第二次信号（此时直径退化为 $F_1$ 的直径 $1495.00 " m"$），最不稳定。

值得注意的是，几何选点法的选点耗时中位数仅 $0.015 " s"$，比平均 GDOP 法的 $0.624 " s"$ 快约 $40$ 倍，而平均直径只差 $15.29 " m"$。因此本文的工程结论是：*以平均 GDOP 为主准则，几何得分可作为超大规模搜索时的快速代理*，二者是精度与速度的取舍而非优劣关系。

另需注意，平均 GDOP 法的最大值仍达 $306.71 " m"$，说明存在个别几何退化场景。*因此实际使用时必须在第二次观测后按硬约束重新计算 $F_2$；若区域仍然过大，应继续增加检测点，而不能仅依据选点得分宣称已完成精确定位。*

#sec[6.9 稳健性与参数敏感性]

#table-title[表 6-4　问题二的主要稳健性检验]

#table(
  columns: (2.2fr, 2.0fr, 2.4fr),
  align: (left, center, left),
  stroke: 0.35pt,

  [检验项], [结果], [结论],

  [候选域取圆内 $slash$ 理论可检测域], [期望直径均为 $44.650 " m"$；实得 $47.788 " m"$；选点位移 $0.0 " m"$；秩相关 $1.0$], [允许圆外候选域不改变选点与结果],
  [采样规模 $80 times 5$ $slash$ $80 times 17$], [选点位移 $0.0 " m"$；实得直径完全相同], [正式采样规模已收敛],
  [误差模型 uniform $slash$ truncated-Gaussian $slash$ endpoint], [实得直径均为 $47.788 " m"$；秩相关 $1.0$], [结论不依赖误差分布形状],
  [接收半径先验 conditional-uniform $slash$ fixed-$1000$], [直径 $47.788 " m"$ $slash$ $46.942 " m"$；选点位移 $41.19 " m"$], [先验改变会移动选点但不恶化结果],
  [$F_1$ 直径分组（按四分位）], [最大组平均 $64.08 " m"$；最小组平均 $30.91 " m"$], [$F_1$ 越大可选点越差，符合几何直觉],
)

#sec[6.10 问题二结论]

第一次检测后可由示向度的 $plus.minus 1^"°"$ 误差范围与目标圆域确定可行域 $F_1$；第二检测点的候选区域为 $C_f = F_1 plus B(0,1500)$，实际搜索域为 $C = Omega inter C_f$，保证接收域为 $C_g$。选点采用最小化平均 GDOP 的准则，$10000$ 个场景下平均定位直径为 $52.38 " m"$，优于几何法、FIM E-最优法与随机法。

*上述结论只适用于本文的候选域、采样设置与误差边界，不能推出平均 GDOP 在一切场景下最优。*无论采用哪种准则，第二次观测后都必须按硬约束重算 $F_2$。

#pagebreak()

// ==================================================
// 七、问题三
// ==================================================

#major[七、问题三：全向干扰源的自动搜索、定位与清除]

#sec[7.1 问题结构与求解思路]

问题三要求"*在确保所有干扰源被清除的前提下*"最小化完成时间。这一表述决定了模型必须是*两层结构*：

- *确定性保证层*：负责"不漏"，由七点覆盖证书、频道状态机、硬可行集更新与最小包围圆清除证书组成。该层不允许任何概率性删减。
- *时间优化层*：只负责访问顺序，通过完成代价预演、顺路观测、滚动重规划与有限试清除压缩移动与检测次数。*该层可以任意改进，但不得改变保证层的任何硬约束。*

这种分层的意义在于：效率优化无论怎么做都不会破坏正确性，因而可以先锁定正确性、再无限调优路线，这也是本文能在 $30$ 组配对验证中做到 $396/396$ 全部清除的原因。

#figure(
  block(
    width: 100%,
    stroke: 0.5pt + luma(40%),
    inset: 8pt,
  )[
    #align(center)[
      *确定性保证层*\
      #text(size: 9pt)[七点覆盖证书 $->$ 频道状态机 $->$ 硬可行集更新 $->$ 最小包围圆清除证书 $slash$ 有限圆盘覆盖兜底]
      #v(0.5em)
      ↓
      #v(0.5em)
      *时间优化层*\
      #text(size: 9pt)[完成代价预演 $->$ 顺路观测与共享停靠 $->$ 动态开放路线滚动重规划 $->$ 有限试清除]
    ]
  ],
  caption: [问题三的两层策略结构。上层只做"是否可靠"的判断，下层只做"是否更快"的判断。],
) <fig-q3-structure>

#sec[7.2 七点覆盖骨架与"不存在"证书]

取原点 $O$ 与半径 $1200 " m"$ 正六边形的六个顶点构成七个搜索点：

$
V_k = 1200 (cos theta_k, sin theta_k), quad theta_k = theta_0 + (k-1) dot.op 60^"°", quad k = 1,dots.c,6,
$ <eq-hex>

记 $A_7 = {O, V_1,dots.c,V_6}$。可以解析地算出目标圆域内任一点到 $A_7$ 中最近点的最坏距离。该最坏值出现在两类位置：内部 Voronoi 顶点，以及目标圆域边界上的弧中点，对应

$
d_v = frac(1200, 2 cos 30^"°") = frac(1200, sqrt(3)) = 692.8203 " m" ,
$ <eq-dv>

$
d_b = sqrt(1800^2 + 1200^2 - 2 dot.op 1800 dot.op 1200 cos 30^"°") = 968.9016 " m" .
$ <eq-db>

故最坏距离为 $968.9016 " m"$，相对最小有效接收半径 $1000 " m"$ 的*裕量为*

$
1000 - 968.9016 = 31.0984 " m" > 0 .
$ <eq-margin>

#small-title[该证书的含义]

由式 @eq-margin，目标圆域内任意位置到最近搜索点的距离都小于 $1000 " m"$。而题面保证所有源的接收半径均不小于 $1000 " m"$，因此*任意位置的任意全向干扰源都必然在至少一个搜索点上被收到*。于是：*若某频道在七个搜索点上全部返回 `no_signal`，则可以严格证明该频道不存在干扰源。*

这一点是问题三"个数未知"这一困难的完整解法。*单个检测点的无信号结果绝不能替代此证书*——一次无信号可能只是距离过远。

#figure(
  block(
    width: 100%,
    stroke: 0.6pt + luma(45%),
    inset: 9pt,
    radius: 2pt,
  )[
    #align(center)[
      #text(size: 10pt, weight: "bold")[【图片占位符】]
      #v(0.30em)
      #text(size: 9.5pt)[七点覆盖布局与最坏覆盖半径]
      #v(0.35em)
      #text(size: 9pt, fill: luma(25%))[
        应绘制：半径 $1800 " m"$ 目标圆域、原点 $O$ 与六个顶点 $V_1$--$V_6$（半径 $1200 " m"$）、
        六边形边与对角线、每个搜索点周围半径 $1000 " m"$ 的必接收圆（半透明填充），
        并标注内部 Voronoi 距离 $d_v = 692.82 " m"$、边界弧中点最坏距离 $d_b = 968.90 " m"$ 与裕量 $31.10 " m"$。
        建议用不同线型区分"必接收圆"与"目标圆域边界"。
      ]
    ]
  ],
  caption: [七点覆盖骨架及其"不存在"证书。七个必接收圆的并集完全覆盖目标圆域，是本问保证不漏的全部依据。],
) <fig-q3-cover>

#small-title[骨架的时间下界]

七点骨架的路线长度为 $1200 + 5 times 1200 = 7200 " m"$，对应移动时间 $1440 " s"$；七个点各扫描 $20$ 个频道，检测耗时 $700 " s"$；同一点内 $19$ 次频道切换，耗时 $133 " s"$。故*仅完成一次完整覆盖扫描的保守下界为*

$
T_("skeleton") >= 1440 + 700 + 133 = 2273 " s" .
$ <eq-skeleton-lb>

这一下界只包含骨架本身的固定开销，不含任何定位与清除。理解它有助于解释后文结果：$3000$--$4000 " s"$ 的总时间是合理量级，而"总时间降到 $2000 " s"$ 以下"在本策略框架内是不可能的。

#sec[7.3 分频道状态机与硬可行集]

每个频道独立维护一个状态，频道号本身即目标身份，从而完全避免匿名数据关联问题：

$
c: "UNKNOWN" arrow.r "FOUND" arrow.r "CLEARED", quad "UNKNOWN" arrow.r "ABSENT" .
$ <eq-state>

对位置可行集 $F_c$，三类观测的更新规则如下（$5 " m"$ 边长的闭方格保守外包，保证不删除任何真实位置）：

- `direction`（示向度 $hat(phi)$）：保留同时满足示向角在 $[hat(phi)-1^"°", hat(phi)+1^"°"]$、位于目标圆域、且 $5 < norm(x-p) <= 1500$ 的方格；
- `no_signal`：*只删除能证明整体位于 $1000 " m"$ 必接收圆内的方格*，绝不按 $1500 " m"$ 删除（依据同假设 A5）；
- `near`（距离不超过 $5 " m"$）：下一动作立即原地 `/clear`。

同一频道在同一位置不重复检测。

#sec[7.4 安全清除证书与兜底有限覆盖]

设频道 $c$ 的保留方格集合为 $P_c$。对所有保留方格的四角求最小包围圆 $"MEC"(P_c) = (m_c, r_c)$。若

$
r_c <= 20 - 0.25 = 19.75 " m" ,
$ <eq-mec>

则 $P_c$ 中任何位置都落在以 $m_c$ 为圆心、半径 $20 " m"$ 的清除半径内，因此在 $m_c$ 处执行 `/clear` *必然成功*——这是一张可验证的证书，而不是一次尝试。

若长期无法达到式 @eq-mec 的阈值（正常示向累计不少于 $6$ 次仍不满足），则退化为*有限圆盘覆盖兜底*：以保留方格中心作为清除点逐一访问。由于方格半对角线为

$
rho = 20 / sqrt(2) = 14.142 " m" < 20 " m" ,
$ <eq-lattice>

这些半径 $20 " m"$ 的清除圆盘之间没有缝隙，且清除点个数有限，故*有限步内必然完成清除*。这就保证了即使示向信息极差，算法也不会陷入无限循环。

#sec[7.5 滚动调度与动态开放路线]

把动作集记为 $A = {"SEARCH"}, "LOCALIZE", "CLEAR"$。每个候选动作的评分由"实际耗时 $+$ 预计剩余时间 $+$ 尾部风险"组成：

$
J(a) = T_("move") + T_("switch") + T_("action") + hat(T)_("remain") + lambda R_(0.9) .
$ <eq-cost-q3>

其中 $hat(T)_("remain")$ 为按当前未完成频道数估计的剩余骨架与收尾时间，$R_(0.9)$ 为 $90%$ 分位的尾部风险惩罚。调度上采用*动态开放路线*：每次只执行排序后的首节点，随后依据真实到达位置和新观测重新规划，避免局部误差累积。

为防止覆盖任务被无休止的局部动作"饿死"，设置防饿死约束：连续局部动作（定位与清除）最多 $L = 3$ 次，达到上限后强制推进下一个搜索点。

#sec[7.6 算法]

#algorithm("算法 3　全向源滚动搜索、定位与清除（candidate_057_posterior_free）", [
  *输入：* $20$ 个频道、七点骨架 $A_7$、题面物理参数。\
  *Step 1.* 调用 `/enter`；在原点 $O$ 处按频道号顺序扫描全部 `unknown` 频道，按观测结果更新状态与可行集。\
  *Step 2.* 依据原点示向结果选择六边形的旋转与遍历方向；依次访问 $V_1$--$V_6$。\
  *Step 3.* 对每个 `found` 频道计算最小包围圆证书；若满足式 @eq-mec 则安排 `CLEAR`，否则生成有限覆盖清除点。\
  *Step 4.* 对每个候选动作按式 @eq-cost-q3 打分排序；若连续局部动作已达 $L = 3$，强制插入一次 `SEARCH`。\
  *Step 5.* 只执行首节点并立即重规划；网络重试复用同一 `request_id` 与请求体。\
  *Step 6.* 依据返回值更新：`direction` 做扇环求交；`no_signal` 只删除 $1000 " m"$ 圆内方格；`near` 原地清除。\
  *Step 7.* 仅当满足终止条件之一时调用 `/exit`。
])

#sec[7.7 正确性、终止条件与复杂度]

正确性由四条引理支撑：

1. 正常示向给出的扇环硬集合始终包含真实位置（由 $plus.minus 1^"°"$ 硬边界保证）；
2. 对全向源，一次无信号可以安全排除 $1000 " m"$ 必接收圆内的全部位置（由最小接收半径保证）；
3. 七个必接收圆覆盖整个目标圆域，故完整扫描后仍为 `unknown` 的频道必然不存在（式 @eq-margin）；
4. 最小包围圆证书或有限圆盘覆盖必给出一个必然成功的清除点（式 @eq-mec、@eq-lattice）。

*终止条件*只有两条：已成功清除上界 $16$ 个源；或完整七点覆盖完成、其余未知频道均被认证为 `ABSENT`、且所有 `found` 均已 `CLEARED`。*不允许*用"连续若干次无新发现"或"已发现 $10$ 个"提前终止——前者没有证书支撑，后者只是题设下界。

复杂度：一次硬更新 $O(N)$（$N$ 为方格数，$5 " m"$ 网格下约 $2.5 times 10^4$），最小包围圆随机增量期望 $O(H)$，候选排序约 $O(K M)$，内存 $O(20 N)$。

#sec[7.8 演练测试与大样本验证]

#small-title[（1）演练测试]

在官方模拟器演练模块进行测试，逐局记录清除比例与平均定位清除时间

$
rho = frac(N_("clear"), N), quad bar(T) = frac(T, N_("clear")) .
$ <eq-rho>

$4$ 局演练的汇总结果为：全清率 $1.0$、清除比例均值 $1.0$、平均虚拟时间 $4912.467 " s"$（中位数 $4729.253 " s"$，$95%$ 区间 $[4409.114, 5621.482]$）、平均定位清除时间 $373.723 " s"/"个"$。

#small-title[（2）配对消融：确定性基线与最终策略]

为隔离时间优化层的作用，在*固定代码、参数与随机种子*的条件下，把最终策略 `candidate_057_posterior_free` 与确定性基线 `candidate_041`（只有保证层，同一七点骨架与同一硬更新规则）做 $30$ 组配对：

#table-title[表 7-1　最终策略与确定性基线的配对验证（$30$ 组，每组 $N$ 个源）]

#table(
  columns: (1.9fr, 0.8fr, 1.0fr, 1.2fr, 1.9fr, 1.1fr),
  align: center,
  stroke: 0.35pt,

  [策略], [场次], [清除源数], [平均总时间/s], [每源时间/s·个⁻¹], [清除失败],

  [041 确定性基线], [30], [396], [3795.54], [287.54], [0],
  [*057 最终策略*], [*30*], [*396*], [*3442.53*], [*260.80*], [*0*],
  [配对改善], [—], [—], [*353.01*], [*26.74*], [—],
)

配对 bootstrap 给出每源节省的 $95%$ 区间为 $[21.402, 32.586] " s"$，*胜率 $30/30$*，最差回退 $0.0 " s"$，相对基线降低 $9.30%$。时间构成上，$057$ 的移动时间由 $2958.61 " s"$ 降至 $2646.79 " s"$，而检测、切频、光学、激光各项几乎不变——说明改善确实来自路线优化而非牺牲定位质量。

#small-title[（3）$100$ 例并发压力测试]

在固定种子 $20261310$、并发 $100$ 的条件下运行最终策略：全部 $100$ 个案例成功，清除 *$1291/1291$* 个源（$100%$），平均虚拟时间 $3305.03 " s"/"例"$，每源 $256.01 " s"$（bootstrap $95%$ 区间 $[250.00, 261.90]$），P95 为 $3703.95 " s"$、最大 $3999.60 " s"$。

#small-title[（4）源数分层与非单调现象]

#table-title[表 7-2　按真实源数分层的每源平均时间（057 与基线，$30$ 组样本）]

#table(
  columns: (1.2fr, 0.9fr, 1.4fr, 1.4fr),
  align: center,
  stroke: 0.35pt,

  [源数 $N$], [场次], [057 每源/s], [041 每源/s],

  [10], [5], [318.66], [337.48],
  [11], [3], [308.15], [342.69],
  [12], [3], [278.04], [307.23],
  [13], [4], [271.11], [291.75],
  [14], [5], [252.86], [278.75],
  [15], [5], [237.32], [262.08],
  [16], [5], [*219.60*], [253.55],
)

可见源数越多、*每源*平均时间反而越低。原因是每源固定开销（骨架扫描的 $2273 " s"$ 级成本）被更多源摊薄。这一现象在问题四会被进一步放大，并在 §8.8 的受控实验中成为"源数效应非单调"的核心机制。

#small-title[（5）离线策略族消融]

在 $100$ 个本地案例上比较九种策略（全部参数与案例相同）：

#table-title[表 7-3　离线策略族消融（每组 $100$ 例，全部 $100/100$ 全清）]

#table(
  columns: (2.2fr, 1.3fr, 1.5fr, 1.5fr, 1.2fr),
  align: center,
  stroke: 0.35pt,

  [策略], [平均总时间/s], [每源时间/s·个⁻¹], [移动时间/s], [清除失败均值],

  [A_geometry], [*4973.81*], [*398.52*], [*3883.01*], [*0.25*],
  [B1 骨架顺路], [5396.27], [431.61], [4238.60], [6.88],
  [B2 硬滚动], [5441.66], [435.04], [4283.10], [6.51],
  [B3 粒子混合], [5463.79], [436.75], [4305.18], [6.48],
  [S_local_limit_1], [5459.45], [436.30], [4299.06], [6.48],
  [S_local_limit_5], [5444.47], [435.49], [4289.79], [6.55],
  [A_e_optimal], [5511.00], [440.49], [4313.39], [20.59],
  [A_expected_diameter], [5792.57], [462.39], [4635.14], [7.40],
  [B0 固定两阶段], [6110.28], [488.12], [5193.80], [7.78],
)

#figure(
  image("../task3/results/figures/offline_virtual_time_boxplot.png", width: 80%),
  caption: [九种离线策略在 $100$ 个案例上的总虚拟时间分布。],
) <fig-q3-boxplot>

#figure(
  image("../task3/results/figures/optimization_057_validation/paired_time_budget.png", width: 80%),
  caption: [057 与确定性基线的五类虚拟时间分解对比。改善集中在移动时间，检测、切频与光学时间基本不变。],
) <fig-q3-budget>

#figure(
  image("../task3/results/figures/optimization_057_validation/paired_actual_routes.png", width: 80%),
  caption: [057 与基线在配对验证中的实际行驶路线对照。],
) <fig-q3-routes>

#small-title[（6）防饿死阈值的消融]

针对"连续局部动作上限 $L$"这一唯一带有主观性的参数，本文做了 $L = 1,3,5$ 的消融：平均总时间分别为 $5459.45$、$5463.79$、$5444.47 " s"$，三者的差异远小于案例间的波动幅度。因此本文*保留设计基线 $L = 3$，但不声称它在均值意义下最优*。

#sec[7.9 正式测试结果]

按题面要求完成三次正式测试，结果按表 1 格式列于表 7-4。

#table-title[表 7-4　问题三正式测试结果]

#table(
  columns: (2.4fr, 1.2fr, 1.6fr, 1.4fr),
  align: center,
  stroke: 0.35pt,

  [测试案例编码], [清除干扰源个数], [平均定位清除时间/s·个⁻¹], [程序运行时间/s],

  [2J4Q-VV9H-9THR-CAS7], [15], [251.294], [3769.413],
  [3Q3K-ZQ65-UUC3-ZD9G], [14], [245.043], [3430.601],
  [FRNS-WU2R-9F43-JY4N], [12], [264.158], [3169.897],
  [*三局合计 / 平均*], [*41*], [*252.925*], [*3456.637*],
)

// TODO(待人工确认)：上表案例编码由"本地运行摘要与正式日志接收时间、清除数、虚拟时间逐一精确匹配"得到
// （匹配源：task3/results/raw/practice/ 下三份 summary.json，其 test_module 字段登记为 problem3_practice）。
// 正式测试日志本身未携带案例编码。请以模拟器日志列表中的官方编码为准，若不一致请直接替换本表第一列。

三局的程序运行时间取日志中最后一条 `/exit` 的虚拟时间，三局均为"正常退出"。*证据边界*：正式测试日志未提供各案例的真实干扰源总数，因此本文*不声称每局全清*，也不以正式三局作为算法优越性的主要依据；三局样本量很小，结果受源数量、空间分布与频道组合的共同影响。正式三局的作用是*如实记录最终运行表现*，大样本可靠性证据由 §7.8 的配对验证与并发测试提供。

#figure(
  block(
    width: 100%,
    stroke: 0.6pt + luma(45%),
    inset: 9pt,
    radius: 2pt,
  )[
    #align(center)[
      #text(size: 10pt, weight: "bold")[【图片占位符】]
      #v(0.30em)
      #text(size: 9.5pt)[问题三正式测试路线图]
      #v(0.35em)
      #text(size: 9pt, fill: luma(25%))[
        应绘制（三局各一张或合并）：半径 $1800 " m"$ 目标圆域与七点骨架；
        机器狗实际行驶折线；叠加标记检测点（`/measure`）、成功清除点、`no_target_in_range` 失败点；
        用不同颜色区分不同频道的定位与清除，并标注每局的最终虚拟时间。
        数据来源：`task3/results/raw/practice/` 对应三份 `.jsonl` 行为日志。
      ]
    ]
  ],
  caption: [问题三正式测试的实际行驶路线与清除点分布],
) <fig-q3-official>

#sec[7.10 敏感性分析]

*本节按约定暂时空缺。*

问题三目前缺少专门的敏感性分析。仓库中已有的、可作为敏感性证据使用的材料包括：

- 源数分层表（表 7-2），覆盖 $N = 10$--$16$；
- 防饿死阈值 $L = 1,3,5$ 的消融（§7.8 第 6 条）；
- 网格步长 $5 " m" / 10 " m" / 15 " m"$ 的策略探针；
- 策略族消融（表 7-3）。

*尚未开展*的敏感性维度包括：示向误差幅度、源的空间分布、有效接收半径分布。补做时建议按问题四 §8.8 的受控全因子设计思路，冻结实例属性后单独扰动上述变量，并同时报告均值差、配对区间与逐例胜率。

#pagebreak()

// ==================================================
// 八、问题四
// ==================================================

#major[八、问题四：全向与定向混合场景下的搜索与清除]

#sec[8.1 定向源带来的无信号歧义]

定向源的有效覆盖范围为定向方向两侧各 $90^"°"$（含），在该范围之外没有信号辐射。于是 `no_signal` 有三种可能成因：

$
"no_signal" quad <=> quad
"该频道不存在" quad or quad
norm(x - p) > R_c quad or quad
abs("wrap"(angle(x-p) - alpha)) > 90^"°" .
$ <eq-nosignal-q4>

#small-title[为什么问题三的方法不能直接沿用]

问题三的判据"七点全无信号 $arrow.r$ 该频道不存在"依赖一个隐含前提：*源只要在接收半径内就一定能被任何方向的检测点收到*。对全向源成立，对定向源不成立——定向源可以让全部七个搜索点都落在其盲区半平面内，而源依然存在。

因此，*问题四必须更换发现层的几何结构，而不是调整参数*。同时，"*一次负观测永远不足以宣称频道缺失*"成为贯穿全章的硬性原则。

#figure(
  block(
    width: 100%,
    stroke: 0.6pt + luma(45%),
    inset: 9pt,
    radius: 2pt,
  )[
    #align(center)[
      #text(size: 10pt, weight: "bold")[【图片占位符】]
      #v(0.30em)
      #text(size: 9.5pt)[定向源的无信号歧义与盲区几何]
      #v(0.35em)
      #text(size: 9pt, fill: luma(25%))[
        应绘制：一个定向源（位置 $x$、定向方向 $alpha$）、以 $alpha$ 为中心 $180^"°"$ 的覆盖扇区（半透明）、
        接收半径圆 $R_c$、若干检测点。用两种颜色分别标出"能收到示向"的检测点与
        "因位于盲区而返回 `no_signal` 但*不可排除*"的检测点，并标注
        $abs("wrap"(angle(x-p)-alpha)) > 90^"°"$ 的判据。
      ]
    ]
  ],
  caption: [定向源下 `no_signal` 的三义性。位于盲区的检测点返回无信号，但源确实存在。],
) <fig-q4-geometry>

#sec[8.2 发现层：25 点双环覆盖证书]

本文用 25 个搜索点构造一个对 $180^"°"$ 半平面同样有效的发现证书。构造方式为：

1. 外环：半径 $R_("out") = 1800 / cos 15^"°" + 0.25 " m"$ 的正十二边形十二个顶点；
2. 内环：半径 $980 " m"$ 的正十二边形十二个顶点，相对外环旋转 $15^"°"$；
3. 原点，共 $12 + 12 + 1 = 25$ 个点。

外环与内环交错排列，与原点共同构成一个*三角剖分*。该剖分具有两条关键性质：

- *性质 1（接收保证）*：每条剖分边的长度均*小于*最小有效接收半径 $1000 " m"$。因此任一干扰源必落在某个三角形内部，而该三角形的三个顶点到源的距离都不超过 $1000 " m"$，即三个顶点都能收到它。
- *性质 2（半平面穿透）*：对任一过源点的闭半平面，该源所在三角形的三个顶点中必有至少一个落在该半平面内。

性质 2 正是针对 $180^"°"$ 定向覆盖的。若检测点位于源的覆盖扇区之外则收不到信号，而覆盖扇区恰是一个 $180^"°"$ 的闭半平面；由性质 2，该三角形中至少有一个顶点落在覆盖扇区内——*于是那个顶点必然收到示向*。这就把"发现定向源"重新变成了一个有证书保证的问题。

#figure(
  block(
    width: 100%,
    stroke: 0.6pt + luma(45%),
    inset: 9pt,
    radius: 2pt,
  )[
    #align(center)[
      #text(size: 10pt, weight: "bold")[【图片占位符】]
      #v(0.30em)
      #text(size: 9.5pt)[25 点双环覆盖布局与剖分边约束]
      #v(0.35em)
      #text(size: 9pt, fill: luma(25%))[
        应绘制：半径 $1800 " m"$ 目标圆域；外环正十二边形（$R = 1800/cos 15^"°" + 0.25$）、
        内环正十二边形（$r = 980 " m"$，旋转 $15^"°"$）、原点；标出三角剖分的全部边并高亮
        "每条边 $< 1000 " m"$"这一约束；可叠加一个示例定向源的覆盖扇区与落在其中的顶点。
      ]
    ]
  ],
  caption: [25 点双环的三角剖分。每条剖分边短于最小接收半径，且任一过源的闭半平面必含一个顶点。],
) <fig-q4-double-ring>

#sec[8.3 清除层：$50 " m"$ 认证光学覆盖]

当某频道的正示向可行多边形已经足够小、能被一个半径 $R = 50 " m"$ 的圆覆盖时，本文执行*认证光学覆盖*：先在圆心尝试 `/clear`（覆盖 $r <= 20 " m"$ 的区域），若返回 `no_target_in_range`，则依次访问位于半径 $r_h = 36 " m"$ 圆周上的十个等角点。

该构造的覆盖证明如下：对 $r in [20, 50]$，最坏情况下源到最近环点的角差不超过 $360^"°" / 10 / 2 = 18^"°"$，故最坏距离为

$
d^2(r) = r^2 + 36^2 - 72 r cos 18^"°" ,
$ <eq-ring-dist>

$d^2(r)$ 关于 $r$ 是凸函数，其最大值在区间端点取得：

$
max(d(20), d(50)) = 19.2924 " m" < 20 " m" .
$ <eq-ring-max>

因此*圆心加十个环点这十一次尝试中，必有一次落在源的 $20 " m"$ 清除半径内*。局部最坏成本为 $82.2486 " s"$（含十次失败的 `/clear` 与一次成功），不含前往圆心的路程。

对更小的可行域采用更省的配置：阈值不超过 $35 " m"$ 时用 $6$ 个环点（半径 $22 " m"$），不超过 $45 " m"$ 时用 $8$ 个环点（半径 $33 " m"$）。

#sec[8.4 清除点补测与启发式排序]

由于 `/clear` 的频道参数不触发切频，且清除点本身就是一次位置更新，本文在实际成功清除的位置对至多 $2$ 个 `active` 频道补做测量。选择补测频道的打分依据为：距离不超过 $1350 " m"$；与已有观测站的交角满足 $max abs(sin) >= 0.25$；预测楔形收缩满足 $r_("after") <= max(R, 0.75 r)$。排序使用 $"visibility" times log(r / r_("after"))$。

需要明确声明：*这一补测仅为启发式排序，不签发任何安全或可见性证书*，它只影响访问顺序。

#sec[8.5 满 16 源后的停止与收尾]

频道号为 $1$--$20$ 且互不相同，源总数上限为 $16$。因此*"已经见过 $16$ 个不同频道"是唯一可证明"不存在更多源"的时刻*。策略据此做两件事：

1. 一旦已见频道数达到 $16$，立即停止对 `unseen` 频道的扫描（记录 `coverage_points_omitted_after_seen16`）；
2. 转入收尾阶段：按当前位置到各目标的距离排序，逐源处理——已定位的直接清除，未定位的调用重捕获子过程（`_NearOpticalReacquireStrategy`）。

若无线重捕获反复失败，则退化为*完整正示向光学条带*：沿第一次正示向方向布置两排间距 $28 " m"$、横向偏移 $plus.minus 14 " m"$ 的候选点，纵向覆盖 $0$ 至 $1500 " m"$，约 $110$ 个候选点，覆盖半径 $sqrt(14^2 + 14^2) < 20 " m"$。这是在"不知道朝向、也无法再靠无线确认"的前提下唯一确定性的清除路径。

#sec[8.6 算法]

#algorithm("算法 4　混合源发现、定位与清除（adaptive_double_ring_clear_probe）", [
  *输入：* $20$ 个频道；仅使用 `/measure` 与 `/clear` 的返回值。\
  *Step 1.* `/enter`；取 25 点双环集合 $A_25$。\
  *Step 2.* 若已见频道数达到 $16$，停止未见扫描并转入 Step 6。\
  *Step 3.* 生成候选节点（未访问覆盖点的 `measure` 与已定位目标的 `clear`），按滚动开放路线（多起点 $2$-opt）排序，*只执行首节点*后重规划。\
  *Step 4.* 若节点为 `clear`：成功则在实际清除位置对至多 $2$ 个 active 频道补测（启发式排序）；失败则将该频道退回 `active`。\
  *Step 5.* 若节点为 `measure`：对每个未退休频道依次测量——`near` 记为已定位；`direction` 追加观测，若可行多边形包围圆半径不超过 $50 " m"$ 则记为已定位；*`no_signal` 只记录，绝不删除覆盖点、绝不判该频道不存在*。\
  *Step 6.* 收尾：对已定位源执行 $50 " m"$ 认证光学清除；对 `active` 源先重捕获，失败则执行完整正示向光学条带。\
  *Step 7.* 全部清除后调用 `/exit`。
])

继承自问题三的关键约束：*覆盖点永不因任何观测被删除*（`max_replaced_waypoints` 强制为 $0$），清除点补测永不删除覆盖点。

#sec[8.7 大样本验证结果]

#small-title[（1）$100 + 100$ 配对实验]

#table-title[表 8-1　混合与全定向场景下的配对结果（各 $100$ 例）]

#table(
  columns: (1.4fr, 1.6fr, 1.2fr, 1.3fr, 1.2fr, 1.2fr),
  align: center,
  stroke: 0.35pt,

  [场景], [策略], [全清], [均值/s], [P95/s], [≤6000 s],

  [混合], [基线], [100/100], [6735.71], [7792.29], [6/100],
  [混合], [*adaptive*], [100/100], [*6436.51*], [*7406.15*], [*15/100*],
  [全定向], [基线], [100/100], [7418.91], [8592.31], [0/100],
  [全定向], [*adaptive*], [100/100], [*7036.46*], [*7909.91*], [*2/100*],
)

配对 bootstrap（$10000$ 次，以 seed 为重采样单元）给出：混合场景平均节省 $299.19 " s"$，$95%$ 区间 $[-381.75, -223.02]$；全定向场景平均节省 $382.45 " s"$，区间 $[-479.22, -285.89]$。两个区间均不跨 $0$，且逐一删除任一案例后结果依然稳健（混合组 $[-307.37, -285.91]$，全定向组 $[-400.61, -366.63]$）。

#small-title[（2）参数锁定后的 $5$ 组共 $1000$ 例验证]

#table-title[表 8-2　五组本地案例的配对验证（全部实现 $100%$ 清除）]

#table(
  columns: (1.7fr, 1.0fr, 1.5fr, 1.5fr, 1.4fr),
  align: center,
  stroke: 0.35pt,

  [场景（案例数）], [基线均值/s], [adaptive 均值/s], [改善/s], [P95 改善/s],

  [混合（300）], [6641.99], [*6377.34*], [264.64], [7808.79 → 7235.65],
  [全定向（300）], [7443.91], [*7056.22*], [387.69], [8851.02 → 8030.95],
  [固定 16 源（200）], [6337.99], [*5742.93*], [*595.06*], [7652.75 → 7044.10],
  [固定 10 源（100）], [6622.21], [*6438.09*], [184.12], [7560.50 → 7197.98],
  [全向（100）], [5590.72], [*5546.52*], [44.20], [6117.99 → 6121.09],
)

时间构成上，混合 $300$ 例的改善主要来自移动（$4741.62 arrow.r 4550.41 " s"$，$-$191.21）与无线测量（$1546.05 arrow.r 1470.02 " s"$，$-$76.03），而光学时间略有上升（$44.25 arrow.r 61.89 " s"$，$+$17.64）——这是用少量光学尝试换取大量移动的典型取舍。

#small-title[（3）必须如实报告的负面结果]

- adaptive *并非逐例占优*：混合 $300$ 例中有 $87$ 例更慢，固定 $16$ 源 $200$ 例中有 $40$ 例更慢。
- 混合场景平均时间仍高于 $6000 " s"$ 达 $377.34 " s"$，全定向场景高 $1056.22 " s"$。
- 单例最大退化达 $1121.93 " s"$（全向组）。

因此在本文中，*"可靠全清"与"$6000 " s"$ 内完成"被明确分开评价*，前者有充分证据，后者没有。

#sec[8.8 敏感性分析]

本节是本文证据强度最高的部分，采用*两层证据*结构：先用既有开发数据做观察性分层，再用*冻结实例属性的全因子受控实验*验证关键结论。

#small-title[（1）方法与证据边界]

第一层（既有数据）：混合 $100$ 例（定向概率 $0.5$，共 $1274$ 源，其中定向 $663$、全向 $611$）与全定向 $100$ 例（$1274$ 源全定向）。两组 seed 编号对齐，但*不是严格的"只翻转类型"配对*——因为条件随机数的消耗会改变后续源的位置与半径，故跨场景差异只能作为*观察性*证据。

第二层（受控实验）：自建生成器，每个基础 seed 先生成*有序的 $16$ 源模板*并冻结全部属性 $(c_j, x_j, y_j, R_j, phi_j, u_j)$；令 $"directional"_j(p) = bold(1)(u_j < p)$。$N = 10, 13$ 取同一模板的前缀，从而"改 $p$ 只改类型，改 $N$ 只增加尾部源"。规模为

$
100 " seeds" times 3 N times 5 p times 2 " strategies" = 3000 " runs" ,
$ <eq-factorial>

构成 $1500$ 对配对案例。该设计提高的是*内部效度*（可归因），不提高外部效度。

统计方法：所有差值*先在 seed 内求差，再以 base_seed 为重采样单元*做 $10000$ 次 bootstrap 百分位 $95%$ 区间（`bootstrap_seed = 20260914`）。时间恒等式在 $3000$ 行上逐行验证，最大绝对残差小于 $10^(-6) " s"$。

#small-title[（2）主要结论一：定向比例是主导敏感因素]

受控实验 $p: 0 arrow.r 1$ 的平均增时如下：

#table-title[表 8-3　定向概率由 $0$ 升至 $1$ 的平均增时（配对 bootstrap 95% 区间）]

#table(
  columns: (0.9fr, 1.9fr, 2.2fr, 2.0fr),
  align: center,
  stroke: 0.35pt,

  [$N$], [基线增时/s], [adaptive 增时/s], [95% 区间（adaptive）],

  [10], [1350.94], [*1098.02*], [[998.36, 1198.95]],
  [13], [1696.11], [*1342.21*], [[1236.15, 1448.49]],
  [16], [2923.98], [*2434.10*], [[2264.69, 2605.93]],
)

逐例符号一致性上，基线在三个 $N$ 下均为 *$100/100$* 全定向更慢；adaptive 在 $N = 10, 13$ 为 $100/100$，$N = 16$ 为 $99/100$。

成本分解显示，增时的 *$65%$--$74%$ 来自移动、$24%$--$30%$ 来自无线测量*、$4%$--$6%$ 来自切频，光学时间几乎不变。这直接指明了优化方向：应压缩高定向比例下的重定位路线与无效测量，而不是压缩光学操作本身。

受控平方和分解进一步给出各效应的相对重要性：

#table-title[表 8-4　处理平方和占比与偏 $eta^2$]

#table(
  columns: (1.6fr, 1.3fr, 1.3fr, 1.3fr),
  align: center,
  stroke: 0.35pt,

  [策略], [因子], [处理 SS 占比], [偏 $eta^2$],

  [基线], [$p$], [*85.73%*], [0.6831],
  [基线], [$N$], [4.16%], [0.0948],
  [基线], [$N times p$], [10.11%], [0.2027],
  [adaptive], [$p$], [*66.61%*], [0.6606],
  [adaptive], [$N$], [24.99%], [0.4220],
  [adaptive], [$N times p$], [8.41%], [0.1972],
)

#figure(
  image("../result/task4_sensitivity_analysis/controlled_experiment/figures/mean_time_response.svg", width: 78%),
  caption: [受控实验的平均虚拟时间随定向概率 $p$ 的响应（六条曲线为两策略 $times$ 三个源数水平）。],
) <fig-q4-mean-response>

#figure(
  image("../result/task4_sensitivity_analysis/controlled_experiment/figures/adaptive_time_saving.svg", width: 78%),
  caption: [adaptive 相对基线的平均节省时间随 $p$ 的变化。优势在高源数、高定向比例时最大。],
) <fig-q4-saving>

#small-title[（3）主要结论二：源数效应非单调]

这是一个反直觉但机制清晰的结果。定义 $N = 16$ 相对 $N = 10$ 的时间差：

#table-title[表 8-5　源数效应 $N = 16$ 减 $N = 10$（负值表示源越多反而越快）]

#table(
  columns: (1.1fr, 2.0fr, 2.0fr),
  align: center,
  stroke: 0.35pt,

  [$p$], [基线差/s], [adaptive 差/s],

  [0], [−1244.51], [−1530.51],
  [0.25], [−576.47], [−1010.37],
  [0.5], [−172.29], [−632.45],
  [0.75], [+98.79（区间跨 0）], [−455.86],
  [1], [*+328.53*], [*−194.44*],
)

机制解释：当定向比例低（$p$ 小）时，全部 $16$ 个源被很快发现，于是*触发"已见 $16$ 源 $arrow.r$ 停止未见扫描"的提前收尾机制*，省去了大量无谓扫描；当定向比例高时，新增源带来的定位与重捕获负担抵消了这一收益，基线甚至转为变慢。机制的直接证据是：adaptive 在 $(N = 10, p = 0)$ 下*末次清除后仍平均运行 $454.91 " s"$*，而在 $N = 16$ 时该量恰为 $0$。

*这意味着不能用单一的"每增加一个源增加多少秒"来建立本问题的性能模型。*若模型把源数当作独立线性负担项，将被本组数据直接反驳；正确做法是显式引入"是否已达已知上限 $16$"这一状态变量，或使用 $N times p$ 交互项。

#small-title[（4）主要结论三：$N times p$ 交互显著]

定义差中差 $I = [T(16,1) - T(16,0)] - [T(10,1) - T(10,0)]$，得

$
I_("基线") = 1573.04 " s", quad "95% CI" [1420.76, 1720.77] ,
$ <eq-interaction>

$
I_("adaptive") = 1336.07 " s", quad "95% CI" [1191.17, 1474.91] .
$ <eq-interaction-2>

两个区间均不跨 $0$。故定向比例与源数*不可独立解释*。

#small-title[（5）策略收益对场景的敏感性]

adaptive 在全部 $15$ 个 $(N,p)$ 格点上平均时间均更低，且每格的配对 bootstrap 区间严格低于 $0$。但收益幅度随场景变化很大：

#table-title[表 8-6　adaptive 的平均节省随场景的变化（$p = 0 arrow.r 1$）]

#table(
  columns: (1.0fr, 1.6fr, 1.6fr, 1.4fr),
  align: center,
  stroke: 0.35pt,

  [$N$], [$p = 0$ 时节省/s], [$p = 1$ 时节省/s], [逐例胜率区间],

  [10], [24.94], [277.86], [56% → 高],
  [13], [56.63], [410.53], [—],
  [16], [310.95], [*800.83*], [— → 88%],
)

在 $(N = 10, p = 0)$ 时 adaptive 只在 $56%$ 的实例中更快，而在 $(N = 16, p = 1)$ 时达到 $88%$。*因此论文必须同时报告均值差、配对区间与逐例胜率*，只报均值会掩盖"简单场景下优势有限"这一事实。

#small-title[（6）阈值敏感性与"达标率"的独立评价]

所有 $3000$ 次运行全部实现 $100%$ 清除，但若把"能否在 $6000 " s"$ 内完成"作为指标，结果对 $N$ 与 $p$ 高度敏感：

#table-title[表 8-7　候选策略在 $6000 " s"$ 内完成的比例]

#table(
  columns: (0.9fr, 1.1fr, 1.1fr, 1.1fr, 1.1fr, 1.1fr),
  align: center,
  stroke: 0.35pt,

  [$N / p$], [0], [0.25], [0.5], [0.75], [1],

  [10], [64%], [21%], [7%], [0%], [0%],
  [13], [96%], [35%], [15%], [4%], [1%],
  [16], [*100%*], [80%], [53%], [30%], [12%],
)

#figure(
  image("../result/task4_sensitivity_analysis/controlled_experiment/figures/within_6000_response.svg", width: 78%),
  caption: [$6000 " s"$ 内完成率随定向概率 $p$ 的响应。最终全清率对 $p$ 不敏感（全为 $100%$），但达标率高度敏感。],
) <fig-q4-6000>

#figure(
  image("../result/task4_sensitivity_analysis/data_only/figures/threshold_sensitivity.svg", width: 78%),
  caption: [不同完成时间阈值下的累计全清率（$5000$--$9000 " s"$）。说明结论不依赖于 $6000 " s"$ 这一单点阈值。],
) <fig-q4-threshold>

#figure(
  image("../result/task4_sensitivity_analysis/data_only/figures/completion_time_ecdf.svg", width: 78%),
  caption: [四组（混合 / 全定向 $times$ 基线 / adaptive）完成时间的经验累积分布。],
) <fig-q4-ecdf>

#figure(
  image("../result/task4_sensitivity_analysis/data_only/figures/mixed_paired_delta.svg", width: 78%),
  caption: [混合样本中定向源占比与"adaptive 减基线"配对时间差的关系（负值表示 adaptive 更快）。],
) <fig-q4-delta>

#small-title[（7）本节的局限]

1. 全部为本地模拟器假设下的结果，*不是官方测试运行*；
2. 第一层 $100 + 100$ 属于策略选择阶段使用的开发数据，其观察性回归受调参选择与小样本影响；
3. 受控生成器改变了潜变量的生成顺序，提高内部效度但*不提高外部效度*；
4. $p$ 是生成阈值，有限样本下的实际定向占比并不严格等于 $p$（实测为 $28.3$--$28.5%$、$51.4$--$52.0%$、$74.9$--$75.4%$）；
5. 仅取 $N = 10, 13, 16$ 三个水平与 $5$ 个 $p$ 水平，不能证明 $N$ 在中间取值处平滑；
6. 全部 $3000$ 次运行均未出现失败，因此*无法从零失败样本推断最坏情况*；$100$ 例零失败对应的单侧 $95%$ 失败率上界仍约为 $2.95%$；
7. P95 在每个格点上仅由 $100$ 例估计，弱于均值，因此本文未把 P99 作为核心结论。

#sec[8.9 正式测试结果]

#table-title[表 8-8　问题四正式测试结果]

#table(
  columns: (2.4fr, 1.2fr, 1.6fr, 1.4fr),
  align: center,
  stroke: 0.35pt,

  [测试案例编码], [清除干扰源个数], [平均定位清除时间/s·个⁻¹], [程序运行时间/s],

  [待补入], [12], [530.537], [6366.440],
  [待补入], [10], [616.129], [6161.285],
  [待补入], [16], [302.271], [4836.335],
  [*三局合计 / 平均*], [*38*], [*456.949*], [*5788.020*],
)

与问题三相同，正式日志未提供案例编码与各案例的真实源总数，故本文不填造编码、不声称每局全清。三局均正常退出，程序运行时间取最后一条 `/exit` 的虚拟时间。

需要强调，问题四正式三局之间差异很大（$4836$--$6366 " s"$），这与 §8.8 得出的"定向比例是主导因素"完全一致：不同案例的定向源比例不同，总时间自然出现数量级相近但显著的差异。*这既不能用来挑选最好成绩代表算法，也不能用来否定算法。*

#figure(
  block(
    width: 100%,
    stroke: 0.6pt + luma(45%),
    inset: 9pt,
    radius: 2pt,
  )[
    #align(center)[
      #text(size: 10pt, weight: "bold")[【图片占位符】]
      #v(0.30em)
      #text(size: 9.5pt)[问题四正式测试路线与定向扇区]
      #v(0.35em)
      #text(size: 9pt, fill: luma(25%))[
        应绘制：25 点双环骨架与目标圆域；机器狗实际行驶折线；
        用实心圆点标记已定位并成功清除的源、空心圆点标记仅被测量过的频道；
        对判定为定向的源叠加 $180^"°"$ 覆盖扇区示意；
        标注每局的最终虚拟时间与清除数。
      ]
    ]
  ],
  caption: [问题四正式测试的行驶路线、清除点与定向源方位示意],
) <fig-q4-official>

#figure(
  image("../experiments/t4_analysis/outputs/official-run-figures/official-run_path.svg", width: 78%),
  caption: [双环策略的一次完整在线运行的行驶路径与测量、清除点分布（本地模拟器，非官方正式测试）。],
) <fig-q4-path>

#sec[8.10 问题四结论]

问题四的核心不是把问题三的无信号规则原样复用，而是*在位置变量之外承认"检测点位于盲区"这一第三种可能*，并用几何证书而不是方向估计来处理它。25 点双环提供了对 $180^"°"$ 半平面同样有效的发现证书；$50 " m"$ 认证光学覆盖把"在未知位置找到源"化为有限次确定性尝试；覆盖点永不因任何观测被删除，是全部正确性的底线。

优化层带来的是可度量但不夸张的收益：混合场景平均节省 $264.64 " s"$（$3.98%$），固定 $16$ 源场景节省 $595.06 " s"$（$9.39%$），且不是逐例占优。敏感性分析表明，定向比例决定了任务时间的量级，而源数通过"发现 $16$ 源后停止搜索"的机制产生非单调效应。

#pagebreak()

// ==================================================
// 九、模型评价与推广
// ==================================================

#major[九、模型的评价、改进与推广]

#sec[9.1 模型优点]

1. *正确性与效率解耦。* 问题三、四把"保证不漏"的证书体系与"只改顺序"的优化层彻底分开，任何效率改动都不能删减保证层约束。这使算法可以持续调优而不必反复重新验证正确性，也是能做到 $396/396$、$1291/1291$ 全部清除的结构性原因。
2. *全程使用硬边界，不用噪声假设。* 题面明确"同地重复测量不改变误差"，本文据此把 $plus.minus 1^"°"$ 当作集合约束而非高斯噪声，避免了给出无法证明的置信区间。
3. *证书可独立验证。* 七点覆盖的 $968.9016 " m" < 1000 " m"$、$50 " m"$ 覆盖的最坏距离 $19.2924 " m" < 20 " m"$、$20 slash sqrt(2) = 14.142 " m"$ 的兜底间隙，都是可以脱离代码、仅凭几何复核的数值。
4. *对失败模式有明确处理。* 无信号、清除失败、定向失锁、近距事件四类异常都有对应规则；兜底有限覆盖保证了算法不会因为示向信息退化而无限循环。
5. *敏感性分析提供了机制而非仅数字。* 问题四的受控全因子实验不仅给出方向（定向比例主导），还给出了机制（增时主要来自移动与测量）与非单调效应的解释（$16$ 源提前收尾），可以直接指导后续优化。

#sec[9.2 模型局限]

#table-title[表 9-1　局限、影响方向与可验证方式]

#table(
  columns: (1.8fr, 2.2fr, 2.4fr),
  align: (left, left, left),
  stroke: 0.35pt,

  [局限], [对结论的影响方向], [如何验证或修正],

  [整个评估建立在本地模拟器上，与官方模拟器的生成机制可能存在差异], [时间绝对值可能整体偏移，但*策略间的相对优劣*更稳健], [用官方演练日志做同口径对比；本文问题三三局正式结果与本地演练处于同一量级],
  [受控实验提高了内部效度但不提高外部效度], [不能由本文数据推断官方实例的源数 / 定向比例分布], [在官方演练中统计可见的源数与定向源数（演练结束界面会显示）],
  [$6000 " s"$ 达标率对 $N,p$ 高度敏感], [不能宣称普遍达标], [按 §8.8 表 8-7 分场景报告，而非给单一达标率],
  [策略并非逐例占优（混合 $87/300$、固定 $16$ 源 $40/200$ 变慢）], [均值改善不保证每个案例改善], [报告逐例胜率与退化最大值，本文已给],
  [$100$ 例零失败无法证明最坏情况], [失败率上界仅约 $2.95%$（单侧 $95%$）], [在压力测试中主动构造极端场景（高定向比例 + 源集中在边界）],
  [问题三缺少专门的敏感性分析], [问题三的结论在示向误差、源分布、接收半径维度上未经检验], [按 §7.10 列出的维度补做受控实验],
)

此外，本文假设机器狗的移动不受地形与障碍约束（题面允许），且忽略了多只机器狗协同的可能——前者与题面一致，后者是模型的适用范围之外。

#sec[9.3 推广]

本文的方法结构可以推广到一类"*在硬误差边界下、以有限步保证发现并处置未知目标*"的问题，例如室内搜救、危险品排查、无人机频谱监测。推广时需要修改的部分是明确的：

1. *几何参数*：目标区域形状（本文为圆域）、最小接收半径（本文 $1000 " m"$）、清除半径（$20 " m"$）需重新计算覆盖证书；本文的 $d_b$、$3 times 5$ 等具体数值不再适用，但"构造一个最坏距离小于最小作用半径的骨架"的思路可直接迁移。
2. *误差模型*：若误差不再是硬边界而是已知分布，集合更新应改为概率更新，此时"证书"变为"概率界"，本文的全部确定性证明需要重写。
3. *目标类型*：若目标不再由互不相同的频道标识，则需引入数据关联，§7.3 的"频道即身份"这一简化失效，会显著提高问题难度。
4. *动作集*：若加入多机器人协同，本文的两层结构仍然适用，但时间优化层从单车路径问题变为多车调度问题。

#pagebreak()

// ==================================================
// 参考文献
// ==================================================

#major[参考文献]

*说明：按约定，参考文献部分允许暂时不完整。以下为仓库中已核实、可直接引用的条目；正文中尚未逐处标注引用位置，定稿前需补齐引用标记并扩充至完整列表。*

+ V. Isler and R. Bajcsy. The Sensor Selection Problem for Bounded Uncertainty Sensing Models. *IEEE Transactions on Automation Science and Engineering*, 3(4): 372--381, 2006. DOI: 10.1109/TASE.2006.876615.

+ G. C. Calafiore. Set-Membership Localization via Range Measurements. arXiv:2603.04867, 2026.

+ H. W. E. Jung. Ueber die kleinste Kugel, die eine räumliche Figur einschliesst. *Journal für die reine und angewandte Mathematik*, 123: 241--257, 1901. DOI: 10.1515/crll.1901.123.241.

+ S. E. Hammel, E. J. Hilliard, K. F. Gong, and P.-T. Liu. Optimal observer motion for localization with bearing measurements. *Computers & Mathematics with Applications*, 18(1--3): 171--180, 1989. DOI: 10.1016/0898-1221(89)90134-X.

+ S. Zhao, B. M. Chen, and T. H. Lee. Optimal sensor placement for target localisation and tracking in 2D and 3D. *International Journal of Control*, 86(10): 1687--1704, 2013. DOI: 10.1080/00207179.2013.792606.

+ S. C. Nardone and V. J. Aidala. Observability criteria for bearings-only target motion analysis. *IEEE Transactions on Aerospace and Electronic Systems*, AES-17(2): 162--166, 1981. DOI: 10.1109/TAES.1981.309141.

+ L. Jaulin. Set-membership localization with probabilistic errors. *Robotics and Autonomous Systems*, 59(6): 489--495, 2011. DOI: 10.1016/j.robot.2011.03.005.

+ D. V. Lindley. On a measure of the information provided by an experiment. *Annals of Mathematical Statistics*, 27(4): 986--1005, 1956. DOI: 10.1214/aoms/1177728069.

+ S. Kullback and R. A. Leibler. On information and sufficiency. *Annals of Mathematical Statistics*, 22(1): 79--86, 1951. DOI: 10.1214/aoms/1177729694.

+ X. Huan and Y. M. Marzouk. Simulation-based optimal Bayesian experimental design for nonlinear systems. *Journal of Computational Physics*, 232: 288--317, 2013. DOI: 10.1016/j.jcp.2012.08.013.

#pagebreak()

// ==================================================
// 附录
// ==================================================

#major[附录]

#sec[附录 A　支撑材料文件列表]

#table-title[表 A-1　支撑材料清单]

#table(
  columns: (2.0fr, 3.6fr),
  align: (left, left),
  stroke: 0.35pt,

  [文件 / 目录], [说明],

  [`task1/src/q1.py`], [问题一的半平面交、圆域裁剪、直径与最小覆盖圆；纯标准库实现],
  [`task1/tests/test_q1.py`], [问题一的 $10$ 项确定性测试，含六边形反例断言],
  [`task1/demo/`], [问题一四张插图的生成脚本],
  [`task2/src/`], [问题二几何、观测更新与十种选点策略],
  [`task2/experiments/`], [问题二的 $10000$ 场景实验、消融、敏感性与绘图脚本],
  [`task3/src/`], [问题三频道状态机、七点覆盖、几何、完成预演与滚动控制器],
  [`task3/experiments/`], [问题三演练入口、离线批量、结果分析与审计脚本],
  [`task3/results/raw/practice/`], [问题三演练 / 在线运行的原始行为日志与逐局摘要],
  [`task4/strategies/adaptive_double_ring_clear_probe.py`], [问题四最终策略],
  [`task4/strategies/double_ring_optical_clear_probe.py`], [25 点双环覆盖与光学条带兜底],
  [`experiments/t4_local/`], [本地问题四评测引擎（含定向源可见性判定）],
  [`result/task4_sensitivity_analysis/`], [问题四敏感性分析：脚本、受控实验数据与图表],
  [`results/q3_q4_formal_test_scores.csv`], [问题三、问题四各三次正式测试的成绩统计],
  [*正式测试行为日志*], [*由模拟器导出后放入支撑材料，文件名不得修改；见题面附录 3、附录 4 要求*],
)

#sec[附录 B　主要程序代码]

*说明：按格式规范，附录需含建模所用全部完整、可运行的源程序代码。此处先给出文件与入口清单，定稿前须将各文件源码完整粘贴或作为附件提交。*

#table-title[表 B-1　复现命令]

#table(
  columns: (2.2fr, 3.4fr),
  align: (left, left),
  stroke: 0.35pt,

  [任务], [命令],

  [问题一测试], [`python -m pytest task1/tests/test_q1.py`],
  [问题二基准], [`python -m task2.experiments.run_all`],
  [问题三离线批量], [`python -m task3.experiments.run_offline`],
  [问题三在线演练], [`python -m task3.experiments.run_practice`],
  [问题四本地批量], [`python -m task4.cli ... --strategy adaptive_double_ring_clear_probe`],
  [敏感性：既有数据], [`python3 -B result/task4_sensitivity_analysis/scripts/analyze_existing.py`],
  [敏感性：受控实验], [`python3 -B result/task4_sensitivity_analysis/scripts/run_controlled_experiment.py --seed-start 60000 --replicates 100 --workers 4`],
  [敏感性：受控分析], [`python3 -B result/task4_sensitivity_analysis/scripts/analyze_controlled.py --experiment-dir <目录>`],
)

#sec[附录 C　复现环境]

- Python 最低版本 $3.11$；主要依赖：NumPy、pandas、SciPy、Matplotlib、scikit-learn、NetworkX、openpyxl、PyYAML。
- 问题二实验随机种子：$20260911$（$400$ 基础状态 $times$ $25$ 次观测 $= 10000$ 场景）。
- 问题三并发测试种子：$20261310$（并发 $100$，$1291$ 个源）。
- 问题三正式测试日志 SHA-256：`fc2e81dfa8fa7c399ac18d14dbf8ec687906da47a78e9a34364523acacc2d464`。
- 问题四受控实验随机种子：基础 seed $60000$--$60099$；bootstrap 种子 $20260914$，重采样 $10000$ 次，重采样单位为 base_seed。
- 问题四受控实验在运行前于 `design.json` 中记录了 $3$ 份源文件与实验脚本的 SHA-256，用于确认"策略未被中途修改"。
- 问题四回归测试：`python3 -B -m unittest discover -s task4/tests -v`，$53/53$ 通过。

#v(1em)

#line(length: 100%, stroke: 0.5pt)

#text(size: 9pt, fill: luma(25%))[
  *初稿状态说明：* 本稿由仓库内已核实的模型、代码与结果产物汇编而成。所有数值均可在引用的来源文件中复核。
  按约定，*问题三的敏感性分析（§7.10）与参考文献* 为允许暂时空缺的两处；
  此外 §7.9 / §8.9 的测试案例编码与全部图片占位符需要人工补充，详见正文中的说明与 `//` 注释。
]
