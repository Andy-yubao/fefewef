// main_condensed.typ
// 问题一压缩版：理论证明与几何图示优先，适合 3--4 页正文。

#set page(
  paper: "a4",
  margin: (
    top: 2.45cm,
    bottom: 2.35cm,
    left: 3cm,
    right: 3cm,
  ),
  numbering: "1",
  number-align: center + bottom,
)

#set text(
  lang: "zh",
  font: ("Times New Roman", "SimSun"),
  size: 10.5pt,
  fill: black,
)

#set par(
  justify: true,
  first-line-indent:(amount: 2em, all:true),
  leading: 0.78em,
  spacing: 0.78em,
)

#set figure(
  gap: 0.45em,
)

#show figure: it => block(
  above: 0.75em,
  below: 0.85em,
  breakable: false,
)[
  #it
]
#show figure.caption: set text(
  font: "SimSun",
  size: 9pt,
)
#show figure: it => block(
  above: 0.35em,
  below: 0.45em,
  breakable:false,
)[
  #it
]

#set math.equation(numbering: "(1)")

// -------------------- 参考论文式层次 --------------------

#let major(title) = block(
  width: 100%,
  above: 1.35em,
  below: 1.00em,
  breakable: false,
)[
  #align(center)[
    #text(
      font: "SimHei",
      size: 14pt,
      weight: "bold",
    )[#title]
  ]
]

#let sec(title) = block(
  above: 1.15em,
  below: 0.72em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 12pt,
    weight: "bold",
  )[#title]
]

#let subsec(title) = block(
  above: 0.95em,
  below: 0.58em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 10.5pt,
    weight: "bold",
  )[#title]
]

#let small-title(title) = block(
  above: 0.78em,
  below: 0.42em,
  breakable: false,
)[
  #text(weight: "bold")[#title]
]

// 仿早年论文的“算法表”：黑白、上下横线，无填充、无圆角。
#let algorithm(title, body) = block(
  width: 100%,
  above: 0.90em,
  below: 0.95em,
)[
  #line(length: 100%, stroke: 0.8pt)
  #v(0.30em)
  #set par(first-line-indent: 0em, leading: 0.65em, spacing: 0.42em)
  #text(size: 9.5pt, weight: "bold")[#title]
  #v(0.25em)
  #line(length: 100%, stroke: 0.45pt)
  #v(0.35em)
  #text(size: 9.5pt)[#body]
  #v(0.30em)
  #line(length: 100%, stroke: 0.8pt)
]

// 三线表标题，保持与 B035 一类论文相近的朴素风格。
#let table-title(body) = block(
  above: 0.80em,
  below: 0.38em,
)[
  #align(center)[#text(size: 9.5pt)[#body]]
]

= 摘要

无线电干扰源的快速定位与清除需要在示向误差、接收距离未知以及干扰源数量和类型未知的条件下，同时解决定位精度与搜索效率问题。本文针对半径为 $1800 " m"$ 的目标区域，建立基于*有界误差定位、观测点优化、确定性覆盖搜索与动态路径规划*的干扰源自动定位清除模型，并分别研究全向干扰源和全向、定向干扰源混合情况下的搜索策略。

*针对问题一，*考虑示向度存在 $±1 "°"$ 的确定误差，将每次测量转化为两个闭半平面约束，通过多站约束相交并与目标圆域精确裁剪，得到干扰源的定位区域。对于纯多边形边界，通过比较顶点对求区域直径；当边界包含圆弧时，进一步考虑圆弧内部可能出现的最远点。计算表明，以定位区域直径为直径的圆一般不能保证覆盖整个定位区域。构造的合法三站算例中，定位区域直径为 $43.3485 " m"$，而最小覆盖圆直径为 $46.1962 " m"$，从而给出了明确反例。

*针对问题二，*首先根据第一次示向结果、目标圆域和 $1000 " m"$ 至 $1500 " m"$ 的有效接收距离确定干扰源可能区域，并据此构造第二检测点的候选区域。在统一候选域内比较 GDOP Mean、Geometry、FIM E 和 Random 四种选点策略，最终采用 GDOP Mean 最小化候选目标位置的平均几何精度因子，并仍以第二次观测后的有界误差定位区域直径评价实际效果。在 $10000$ 个统一测试场景中，GDOP Mean 的平均定位区域直径为 $52.38 " m"$，低于 Geometry 的 $67.67 " m"$、FIM E 的 $193.41 " m"$ 和 Random 的 $588.04 " m"$，表明其能够获得较稳定的二站交会定位效果。

*针对问题三，*利用全向干扰源最小有效接收半径为 $1000 " m"$ 的条件，构造由原点和半径 $1200 " m"$ 正六边形六个顶点组成的七点全频道搜索结构，其最坏覆盖距离为 $968.902 " m" < 1000 " m"$，从而保证完整搜索后能够判断未知频道是否存在干扰源。在此基础上，对各频道分别维护硬可行区域，利用 $5 " m"$ 网格保守更新示向、无信号和近距离信息，并以安全最小包围圆判断是否满足 $20 " m"$ 清除条件。同时将搜索点、定位点和清除点统一进行滚动路径规划，并通过完成代价预演、共享检测点和有限试清除减少重复移动。最终策略 Candidate 057 在 $30$ 组配对测试中完成 $30/30$ 个案例、清除 $396/396$ 个干扰源，平均耗时为 $260.80 " s/个"$，较确定性基线降低 $9.30%$。

*针对问题四，*考虑定向干扰源存在 $180 "°"$ 有效覆盖范围，单次无信号不能再用于直接判定附近不存在目标，因此在问题三的基础上重新设计发现和清除策略。采用 $25$ 点双环搜索保证对未知定向源的持续发现，并利用正示向信息逐步缩小其位置范围；当整个可行区域能够被半径 $50 " m"$ 的圆覆盖时，先在圆心尝试清除，失败后依次访问半径 $36 " m"$ 的十个等角环点，由几何关系可保证该局部区域内的干扰源最终进入 $20 " m"$ 清除范围。进一步结合清除点补充测量和动态路线重排减少移动与检测次数。参数锁定后的五组共 $1000$ 个本地验证案例均实现全部清除；其中混合场景平均任务时间由 $6641.99 " s"$ 降至 $6377.34 " s"$，固定 $16$ 个干扰源的混合场景由 $6337.99 " s"$ 降至 $5742.93 " s"$，说明所设计策略在保证可靠搜索与清除的同时能够有效缩短任务时间。

*关键词：* 无线电干扰源；交会定位；有界误差；GDOP；覆盖搜索；动态路径规划；定向干扰源


= 一、问题重述

== 1.1 问题背景

无线电干扰源的精确定位通常需要技术人员进入目标分布区域，通过多次方位检测逐步缩小干扰源的位置范围。题目拟使用机器狗代替人工完成检测、定位和清除，并规定所有干扰源均位于以原点 $O$ 为圆心、半径 $R_0=1800 " m"$ 的目标圆域内。

测向机在检测点得到的示向度与真实方位角之间存在不超过 $1 "°"$ 的误差，各干扰源的有效接收半径位于 $1000 " m"$ 至 $1500 " m"$ 之间。机器狗能够通过多次检测获得示向信息，并在距离干扰源不超过 $20 " m"$ 时利用光学设备完成精确定位和清除。

本文依次研究四个问题。问题1根据多个检测点的示向度确定干扰源的定位区域，并计算区域直径及判断相应圆的覆盖关系；问题2在已得到一次全向干扰源示向度的条件下选择第二个检测点；问题3研究全向干扰源数量未知时的自动搜索、定位和清除策略；问题4进一步考虑定向干扰源造成的无信号歧义，并重新设计发现和清除方法。

== 1.2 问题重述

=== （1）问题1

已知若干检测点的位置及同一干扰源关于各检测点的示向度。由于每次示向度均存在 $plus.minus 1^"°"$ 的误差，因此每次检测只能给出一个角度范围。需要将各次检测结果与目标圆域共同考虑，确定干扰源的最终定位区域，并求出该区域内任意两点之间距离的最大值。

在得到定位区域直径后，还需要判断是否存在一个以该直径为直径的圆，可以覆盖整个定位区域。

=== （2）问题2

已知某全向干扰源在第一个检测点 $S_1$ 处能够正常得到示向度。需要根据第一次检测得到的位置范围确定第二个检测点的候选区域，并从中选择较好的位置 $S_2$，使第二次检测后形成的定位区域尽可能小。

第二检测点既要考虑能否接收到信号，也要考虑两次示向方向的交会角度。本文比较不同的几何选点方法，并以第二次观测后的定位区域直径作为最终评价依据。

=== （3）问题3

目标区域中共有 $10$ 至 $16$ 个全向干扰源，各干扰源所在频道互不相同，但具体数量和频道均未知。机器狗从原点出发，需要依次完成频道搜索、干扰源定位和清除，并在保证全部干扰源均被清除的前提下尽量缩短任务时间。

因此，本问需要同时解决两个问题：一是如何保证没有遗漏尚未发现的频道；二是在已经发现多个干扰源后，如何安排后续检测点、清除点和行驶路线，减少重复移动和检测。

=== （4）问题4

问题4中同时存在全向干扰源和定向干扰源，干扰源总数仍为 $10$ 至 $16$ 个，但定向源的数量及发射方向均未知。

对于定向干扰源，无信号既可能由距离过远造成，也可能是检测点位于其信号覆盖方向之外，因此不能沿用问题3中根据单次无信号直接排除附近位置的方法。本问需要重新设计搜索覆盖和清除策略，在这种无信号歧义下仍保证所有干扰源最终被发现并清除。

= 二、问题分析

== 2.1 问题1的分析

问题1要求根据多个检测点的示向度确定干扰源的定位区域，并求该区域的直径。由于示向度存在 $plus.minus 1^"°"$ 的误差，每次检测得到的不是一条确定射线，而是一个角度范围。因此，可将各检测点对应的角区域取交，并结合干扰源位于目标圆域内的条件得到最终定位区域。在此基础上，根据定位区域边界的几何形式计算区域直径，并进一步判断以该直径为直径的圆能否覆盖整个定位区域。

== 2.2 问题2的分析

问题2需要在第一次检测结果已知的条件下选择第二个检测点。第二检测点一方面应位于能够有效接收到干扰源信号的位置，另一方面应使两次示向方向具有较好的交会关系，从而减小第二次检测后的定位区域。因此，先利用第一次示向结果和有效接收距离确定第二检测点的候选范围，再根据两次观测的几何关系评价各候选点，并选择定位效果较好的位置。

== 2.3 问题3的分析

问题3中干扰源均为全向源，但干扰源数量及所在频道未知。要保证所有干扰源均被清除，首先需要设计能够覆盖整个目标区域的检测点，使存在干扰源的频道能够被发现；随后根据各次示向结果不断缩小相应干扰源的位置范围，并在满足清除条件后实施清除。由于任务时间还包括机器狗的移动、检测和频道切换时间，因此还需要合理安排各检测点和清除点的访问顺序，以减少不必要的移动和重复检测。

== 2.4 问题4的分析

问题4加入了定向干扰源。对于定向源，检测点无信号既可能是距离过远，也可能是检测点位于其覆盖方向之外，因此问题3中根据无信号排除附近区域的方法不能直接使用。为保证定向干扰源不被遗漏，需要增加不同位置和方向上的搜索检测，并主要利用能够获得示向度的检测结果逐步缩小目标位置范围。在完成目标发现后，再结合光学精确定位范围设计清除点，并对搜索、定位和清除过程统一安排路线。


= 三、模型假设

- 干扰源、检测点和机器狗均按题目要求视为同一水平面内的点，忽略高度差异。一次测试过程中干扰源的位置、频道、有效接收半径及定向方向均保持不变，不同干扰源之间的信号互不影响。

- 示向度误差严格采用题目给出的硬边界 $[-1 "°", 1 "°"]$。同一地点的电磁环境固定，因此在同一位置对同一频道重复测量不产生新的独立误差信息，不能通过重复测量缩小该误差范围。

- 所有干扰源均位于半径 $1800 " m"$ 的目标圆域内，但机器狗和检测点的位置不受该圆域限制。若目标圆域外的位置能够获得更好的交会条件，允许机器狗移动到圆域外检测。

- 每个频道最多对应一个干扰源，有效接收半径为测试过程中固定但未知的量，其范围为 $1000 " m"$ 至 $1500 " m"$。问题3中的干扰源均为全向源；问题4中的定向源具有 $180 "°"$ 的有效覆盖角度，但具体方向未知。

- 对全向干扰源，若检测点与干扰源距离不超过 $1000 " m"$，则该点一定能够接收到信号，因此问题3中的无信号可以安全排除这一范围。对定向干扰源，该结论不成立，因此问题4不根据一次无信号直接判断频道不存在。

- 机器狗只利用 `/measure` 和 `/clear` 等接口实际返回的信息进行决策，不使用模拟器中隐藏的干扰源数量、位置、接收半径和定向方向。成功清除只以接口返回结果为准。


= 四、符号说明

#table(
columns: (1.15fr, 4.8fr, 1.45fr),
inset: 5pt,
align: (center, left, center),
stroke: 0.5pt,
[*符号*], [*含义*], [*单位/说明*],
[$O$], [目标圆域的圆心、坐标原点], [m],
[$R_0$], [目标圆域半径], [$1800 " m"$],
[$G$], [干扰源的真实位置], [m],
[$X$], [干扰源位置的任一候选点], [m],
[$S_i$], [第 $i$ 个检测点，$S_i=(x_i,y_i)$], [m],
[$hat(theta)*i$], [在 $S_i$ 处测得的示向度], [°],
[$epsilon$], [示向度最大绝对误差], [$1 "°"$],
[$W_i$], [第 $i$ 次示向度对应的角可行区域], [-],
[$P$], [多个示向区域相交得到的交会多边形], [-],
[$Omega$], [问题1的最终定位区域 $P ∩ B_0$], [-],
[$D(A)$], [区域 $A$ 内任意两点距离的最大值], [m],
[$B(C,r)$], [以 $C$ 为圆心、$r$ 为半径的闭圆域], [-],
[$r_n$], [检测返回近距离的距离阈值], [$5 " m"$],
[$R*("min"),R_("max")$], [有效接收半径的题设下界和上界], [$1000,1500 " m"$],
[$F_1$], [第一次示向度后的干扰源可行区域], [-],
[$C_f$], [第二检测点可能接收到信号的候选区域], [-],
[$C_g$], [第二检测点能够保证满足最小接收距离的区域], [-],
[$J$], [两次方位观测对应的信息矩阵], [-],
[$G(S_2)$], [候选第二检测点的平均 GDOP 指标], [-],
[$V_j$], [问题3七点搜索中的第 $j$ 个搜索点], [m],
[$Q_k$], [第 $k$ 个频道当前的位置可行区域], [-],
[$rho$], [问题3用于认证安全清除的包围圆半径阈值], [$19.75 " m"$],
[$A_7$], [问题3的七点搜索点集合], [-],
[$A_25$], [问题4的25点双环搜索点集合], [-],
[$r_c$], [光学精确定位与清除半径], [$20 " m"$],
[$r_o$], [问题4局部光学覆盖的认证半径], [$50 " m"$],
[$r_h$], [问题4十个环形清除点的布置半径], [$36 " m"$],
[$T$], [机器狗执行任务的虚拟时间], [s],
)



#major[五、问题一：多点交会定位区域的直径与圆覆盖判别]


#sec[5.1 示向误差与定位区域的构造]

由题干可知，各检测点的示向度给出的干扰源位置范围是其读数两侧各偏转 $1^"°"$ 的方向范围。因此，利用多点交会法，将各检测点的方向范围与题目中干扰源自身的位置范围取交集，即可得到干扰源的定位区域。


#subsec[5.1.1 单个检测点对应的定位范围]

设第 $i$ 个检测点为 $S_i=(x_i,y_i)$，测得的示向度为 $hat(theta)_i$，误差上界为

$
epsilon=1^"°".
$

则干扰源真实方位角应满足

$
theta_i^- = hat(theta)_i-epsilon,
quad
theta_i^+ = hat(theta)_i+epsilon.
$ <eq-angle>

记方向角 $theta$ 对应的单位向量为

$
u(theta)=(cos theta,sin theta),
$

并定义二维向量叉积来刻画位置关系

$
a times b=a_x b_y-a_y b_x.
$ <eq-cross>

则对任意待判断位置 $X=(x,y)$，向量 $X-S_i$ 表示从检测点 $S_i$ 指向该位置的方向。

考虑真实方向与示向度的误差， 则$X$ 与第 $i$ 个检测点给出的定位范围有交集，当且仅当

$
u(theta_i^-) times (X-S_i) >= 0,
quad
u(theta_i^+) times (X-S_i) <= 0.
$ <eq-halfplanes>

 @eq-halfplanes 刻画了两个闭半平面，令其交集记为 $W_i$。则问题一中，一个观测点给出的干扰源位置范围就是 $W_i$。从几何的角度，$W_i$ 是以 $S_i$ 为顶点、夹角为 $2epsilon$ 的扇形区域。

#figure(
  image("../paper_assets/paper_figures/section5/fig1_single_bearing_wedge.pdf", width: 56%),
  caption: [单个检测点的示向误差约束],
) <fig-single-wedge>


#subsec[5.1.2 多个检测点的交会区域]

设共有 $m$ 个检测点，则各可行区域取交得到

$
P=W_1 inter W_2 inter dots.c inter W_m.
$ <eq-intersection>

由于每个 $W_i$ 都是凸集，可知交会区域 $P$ 仍为凸集。

求解 $P$ 时，设所有 @eq-halfplanes 表示的半平面边界总数为 $h=2m$。先枚举任意两条非平行边界直线的交点，再检查该交点是否满足全部 $h$ 个半平面约束；保留所有可行交点后求凸包，即可得到 $P$ 的有序顶点。

边界直线共有 $O(h^2)$ 个候选交点，每个交点需要检查 $O(h)$ 个约束，因此该过程的最坏时间复杂度为

$
O(h^3).
$

下@fig-multi-intersection 中的六边形例子就是由六条有效边界相交后形成的凸定位区域范围。

#figure(
  image("../paper_assets/paper_figures/section5/section5_fig2_multi_station_intersection.pdf", width: 82%),
  caption: [多检测点示向约束的交会区域],
) <fig-multi-intersection>


#subsec[5.1.3 干扰源自身的位置范围]

已知所有干扰源均位于以原点 $O$ 为圆心、半径$R_0=1800"m"$的目标圆域内。

记该圆域为 $B(O,R_0)$，则最终定位区域为

$
Omega=P inter B(O,R_0).
$ <eq-omega>

若 $P$ 的全部顶点均位于 $B(O,R_0)$ 内，由 $P$ 的凸性可知整个 $P$ 都位于圆内，此时

$
Omega=P.
$

若存在 $P$ 的顶点落在 $B(O,R_0)$ 外，则需要计算交集边界 $partial Omega$。可得知 $partial Omega$ 由若干线段和圆弧组成，且保持凸性。

#figure(
  image("../paper_assets/paper_figures/section5/section5_fig3_active_target_disk_clipping.pdf", width: 66%),
  caption: [目标圆域对交会区域的裁剪],
) <fig-disk-clipping>


#sec[5.2 定位区域直径的求解]

得到最终定位区域 $Omega$ 后，需要计算区域内任意两点之间的最大距离。定义

$
D=max_(X,Y in Omega) norm(X-Y).
$ <eq-diameter>

由于 $Omega$ 为有界凸集，其直径一定可以在边界上取得。因此，只需根据边界是纯多边形还是含有圆弧进行分类讨论。


#subsec[5.2.1 边界为多边形的情形]

当 $Omega$ 为凸多边形时，由于凸多边形中最远的两点一定可以取为两个顶点，因此设其顶点依次为

$
V_1,V_2,dots.c,V_n,
$

故只需比较所有顶点对之间的距离

$
d_(j k)=norm(V_j-V_k),
quad 1 <= j < k <= n.
$

并取其中最大值即可得到

$
D=max_(1 <= j < k <= n) d_(j k).
$

同时记录取得最大距离的一组顶点，记为 $A,B$，则

$
norm(A-B)=D.
$


#subsec[5.2.2 边界中包含圆弧的情形]

若 $partial Omega$ 同时包含线段和圆弧，此时不能只比较原多边形顶点，还需要考虑圆弧上的极值点。

对一条线段而言，当另一点固定时，距离在线段上的最大值一定在端点取得。因此，线段部分只需保留端点。

对于半径为 $R_0$ 的圆弧，当另一点固定时，圆弧上的最远点只能是圆弧端点，或者位于从圆心 $O$ 向该点反方向延伸的圆周位置。若比较两段圆弧，还需检查两条圆弧中是否包含一对对径点。

因此，连续边界上的直径计算仍可以化为有限个候选点或候选点对的比较，而不需要对整条圆弧进行离散采样。


#algorithm("算法 1　定位区域直径计算", [
  *输入：* 检测点 $S_i$、示向度 $hat(theta)_i$、误差上界 $epsilon$、目标圆域半径 $R_0$。\
  *Step 1.* 根据@eq-halfplanes，将每次示向范围转化为两个闭半平面。\
  *Step 2.* 枚举半平面边界的交点并检查可行性，对可行点求凸包，得到交会区域 $P$。\
  *Step 3.* 计算 $Omega=P inter B(O,R_0)$；若圆域截断 $P$，则求出有效线段、圆弧及其端点。\
  *Step 4.* 对线段—线段、线段—圆弧和圆弧—圆弧三类边界组合分别计算可能的最大距离，其中圆弧补充反向极值点和可行对径点。\
  *Step 5.* 比较全部候选距离，取最大值作为区域直径 $D$，并记录一组直径端点 $A,B$。\
  *Step 6.* 令 $C_D=frac(A+B,2)$，检查 $Omega$ 是否全部位于圆 $B(C_D,D/2)$ 内；若是，则直径圆能够覆盖定位区域，否则不能覆盖。
])


#sec[5.3 以区域直径为直径的圆的覆盖性问题]

求得区域直径 $D$ 后，需要回答题目中的第二个问题：是否一定存在一个直径为 $D$ 的圆覆盖整个定位区域。下面先确定这种覆盖圆可能的圆心，再进行覆盖判别。


#subsec[5.3.1 直径圆圆心的确定]

设 $A,B in Omega$ 为一组直径端点，$norm(A-B)=D$；若存在直径为 $D$ 的圆同时覆盖 $A,B$，则由三角不等式的等号条件可知其圆心只能为两点中点 $C_D=frac(A+B,2)$，因此判断直径圆能否覆盖 $Omega$ 时只需检验这一圆。


#subsec[5.3.2 直径圆的覆盖判别]

由上文可知，直径圆能够覆盖定位区域，当且仅当

$
max_(X in Omega) norm(X-C_D) <= frac(D,2).
$ <eq-cover>

由于 $Omega$ 为凸集，$
max_(X in Omega) norm(X-C_D)$可以在边界 $partial Omega$ 上取得。则对边界类型进行分类讨论：

(i) 若边界为线段，则需检查线段端点

(ii) 若边界包含圆弧，则需检查圆弧端点以及圆弧上距 $C_D$ 最远的反向点。

无法覆盖的反例如下@fig-multi-intersection 所示：

@fig-multi-intersection 为多点交会形成的凸六边形。对该六边形执行上文算法 1，即可求得一组直径端点 $A,B$，其对应圆心为 $C_D$，半径为 $D/2$。绘出其直径圆，如@fig-counterexample ，可以看到六边形仍有边界顶点位于该圆之外。

#figure(
  image("../paper_assets/paper_figures/section5/fig4_hexagon_counterexample.pdf", width: 72%),
  caption: [六边形定位区域的直径圆覆盖反例],
) <fig-counterexample>

即，对该六边形有

$
max_(X in Omega) norm(X-C_D) > frac(D,2),
$

故直径为 $D$ 的圆不能覆盖整个定位区域。由此可知，“区域直径为 $D$”只能说明区域内任意两点的距离不超过 $D$，并不能保证直径为 $D$ 的圆一定能够覆盖整个区域。


#subsec[5.3.3 最小覆盖圆与区域直径的关系]

若允许重新选择圆心，并适当增大圆的半径，记能够覆盖 $Omega$ 的最小圆半径为 $R_("min")$。由于任意覆盖圆必须同时包含直径端点 $A,B$，因此首先有

$
R_("min") >= frac(D,2).
$

另一方面，平面 Jung 定理给出

$
R_("min") <= frac(D,sqrt(3)).
$

于是

$
frac(D,2)
<=R_("min")
<=frac(D,sqrt(3)),
$

等价地，

$
D
<=2R_("min")
<=frac(2D,sqrt(3))
approx 1.155D.
$ <eq-jung>

易得，正三角形可以达到 Jung 定理的上界。尽管正三角形的交会区域属于比较特殊的情形，这仍可以说明当直径圆不能覆盖有界凸性区域时，所需最小覆盖圆直径仍有明确的理论上界。

对于@fig-counterexample 中的六边形，由于半径 $D/2$ 的圆已经不能覆盖该区域，因此必有

$
R_("min")>frac(D,2),
$

即

$
2R_("min")>D.
$

这也从另一个角度说明：定位区域的直径与其最小覆盖圆直径一般并不相等。


#sec[5.4 问题一结论]

每个检测点的示向度及其 $plus.minus 1^"°"$ 误差可以转化为两个半平面约束；将全部约束取交集，并与半径为 $1800"m"$ 的目标圆域取交集，即得到最终定位区域 $Omega$。根据边界中线段和圆弧的极值位置，可以进一步计算区域直径 $D$。

对于任意一组直径端点 $A,B$，若存在直径为 $D$ 的圆能够覆盖 $Omega$，其圆心只能为 $C_D=(A+B)/2$。

而反例表明，该圆仍可能有定位区域的顶点落在圆外。

因此，问题一的结论为：*多点交会可以求得定位区域及其直径，但以定位区域直径为直径的圆不能保证覆盖整个定位区域。*
#set page(
  paper: "a4",
  margin: (
    top: 2.45cm,
    bottom: 2.35cm,
    left: 3cm,
    right: 3cm,
  ),
  numbering: "1",
  number-align: center + bottom,
)

#set text(
  lang: "zh",
  font: ("Times New Roman", "SimSun"),
  size: 10.5pt,
  fill: black,
)

#set par(
  justify: true,
  first-line-indent: (amount: 2em, all: true),
  leading: 0.78em,
  spacing: 0.78em,
)

#set figure(gap: 0.45em)

#show figure: set block(
  above: 0.35em,
  below: 0.45em,
  breakable: false,
)

#show figure.caption: set text(
  font: "SimSun",
  size: 9pt,
)

#set math.equation(numbering: "(1)")


// -------------------- 标题格式 --------------------

#let major(title) = block(
  width: 100%,
  above: 1.35em,
  below: 1em,
  breakable: false,
)[
  #align(center)[
    #text(
      font: "SimHei",
      size: 14pt,
      weight: "bold",
    )[#title]
  ]
]

#let sec(title) = block(
  above: 1.15em,
  below: 0.72em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 12pt,
    weight: "bold",
  )[#title]
]

#let subsec(title) = block(
  above: 0.95em,
  below: 0.58em,
  breakable: false,
)[
  #text(
    font: "SimHei",
    size: 10.5pt,
    weight: "bold",
  )[#title]
]

#let algorithm(title, body) = block(
  width: 100%,
  above: 0.90em,
  below: 0.95em,
)[
  #line(length: 100%, stroke: 0.8pt)
  #v(0.30em)

  #set par(
    first-line-indent: 0em,
    leading: 0.65em,
    spacing: 0.42em,
  )

  #text(size: 9.5pt, weight: "bold")[#title]

  #v(0.25em)
  #line(length: 100%, stroke: 0.45pt)
  #v(0.35em)

  #text(size: 9.5pt)[#body]

  #v(0.30em)
  #line(length: 100%, stroke: 0.8pt)
]

#let table-title(body) = block(
  above: 0.80em,
  below: 0.38em,
)[
  #align(center)[
    #text(size: 9.5pt)[#body]
  ]
]


// ==================================================
// 问题二
// ==================================================

#major[六、问题二： 第二检测点选择及定位区域优化]


#sec[6.1 第一次示向约束与定位区域]

已知第一次检测点 $S_1$ 及测得的示向度 $theta_1$。由于示向度与真实方位角之间的误差不超过 $plus.minus 1 degree$，第一次检测后不能确定干扰源的唯一位置，只能得到其可能位置范围。

设目标圆域为 $Omega$，干扰源位置为 $G$。首先定义两个方位角 $alpha$ 和 $beta$ 的最小角度差为

$ delta(alpha, beta)
  = min(
      abs(alpha - beta),
      360 degree - abs(alpha - beta)
    ).
$ <eq-angle-diff>

其中，$delta(alpha,beta)$ 的取值范围为 $[0 degree,180 degree]$。例如，$359 degree$ 与 $1 degree$ 的角度差为 $2 degree$。

设 $beta(S,g)$ 表示从检测点 $S$ 指向位置 $g$ 的方位角。在检测点 $S$ 测得示向度 $theta$ 后，与该观测相符的位置集合为

$ W(S, theta)
  = {
      g :
      5 < norm(g - S) <= 1500,
      delta(beta(S, g), theta) <= 1 degree
    }.
$ <eq-w>

式 @eq-w 中，距离下界 5 m 是由于距离干扰源不超过 5 m 时无法获得示向度；距离上界 1500 m 为题目给出的最大有效接收半径。

由于干扰源还必须位于半径为 1800 m 的目标圆域 $Omega$ 内，因此第一次检测后的定位区域为

$ F_1 = Omega inter W(S_1, theta_1).
$ <eq-f1>

后续第二检测点的选择均以 $F_1$ 为依据。按照问题一的定义，区域 $A$ 的直径为

$ D(A)
  = max_(p, q in A) norm(p - q).
$ <eq-q2-diameter>

$D(A)$ 越小，表示干扰源的位置范围越小。

 #figure(rect(width:78%, height:22%, stroke:0.7pt + luma(60%))[#align(center)[*图片占位符* \\ 建议内容：第一次定位区域与第二检测点候选区域]], caption: [第一次定位区域与第二检测点候选区域])


#sec[6.2 第二检测点候选区域]

第二次检测点应与第一次定位区域保持适当距离。由于干扰源的最大有效接收半径为 1500 m，若检测点到 $F_1$ 中任意位置的距离均超过 1500 m，则第二次检测一定无法接收到信号。

因此，第二检测点的理论候选区域定义为

$ C_f
  = {
      s :
      min_(g in F_1) norm(s - g) <= 1500
    }.
$ <eq-cf>

考虑机器狗仍在目标圆域内选取检测点，实际搜索区域取为

$ C = Omega inter C_f.
$ <eq-candidate>

还可以定义保证接收到信号的区域

$ C_g
  = {
      s :
      max_(g in F_1) norm(s - g) <= 1000
    }.
$ <eq-cg>

这是因为所有干扰源的有效接收半径均不小于 1000 m。若 $s in C_g$，则无论干扰源实际位于 $F_1$ 中何处，第二次检测都可以收到信号。

但 $C_g$ 的范围通常较小，可能排除交会角较好的检测点。因此，实际选点仍在区域 $C$ 内进行，$C_g$ 只用于说明能够保证接收信号的位置范围。


#sec[6.3 第二次检测后的定位区域的更新]

设第二检测点为 $s$。第二次检测可能出现三种情况：正常获得示向度、未检测到信号以及距离干扰源不超过 5 m。

若正常获得第二次示向度 $theta_2$，则干扰源同时满足两次检测条件，因此新的定位区域为

$ F_2
  = F_1 inter W(s, theta_2).
$ <eq-f2-bearing>

若未检测到信号，由于干扰源的实际有效接收半径未知，不能直接排除距离 $s$ 小于 1500 m 的全部位置。但是题目保证所有干扰源的有效接收半径均不小于 1000 m，因此可以确定

$ norm(G - s) > 1000. $

于是新的定位区域为

$ F_2
  = F_1 without B(s, 1000),
$ <eq-f2-nosignal>

其中 $B(s,1000)$ 表示以 $s$ 为圆心、1000 m 为半径的圆盘。

若检测点与干扰源距离不超过 5 m，则可直接采用光学探测仪进行精确定位。此时可认为干扰源位置已经确定，定位区域直径为

$ D(F_2) = 0.
$ <eq-f2-near>

因此，第二次检测后的定位区域可写为

$ F_2 = cases(
    F_1 inter W(s, theta_2)
      & "获得示向度",
    F_1 without B(s, 1000)
      & "未检测到信号",
    {G}
      & "距离不超过 5 m",
  ).
$ <eq-update>


#sec[6.4 第二检测点选择方法]

第二检测点需要同时满足三个条件：有较大的机会获得第二次示向、两次示向线的交会角不能过小、机器狗的移动距离不能过长。因此，本文采用基于交会质量和接收条件的几何选点方法。

在 $F_1$ 内按面积均匀选取 $N_G$ 个可能位置 $g_j$，对候选点 $s$ 计算第一次与第二次视线的夹角 $alpha_j(s)$，并记 $d_{1j}=norm(g_j-S_1)$、$d_{2j}(s)=norm(g_j-s)$。考虑方位误差在距离较大时造成的横向偏差，定义候选点的几何得分为

$ U_"geo"(s)=frac(1,N_G) sum_(j=1)^(N_G) 1_(d_(2j)(s)<=R_j) frac(abs(sin alpha_j(s)),sqrt(max(d_(1j)d_(2j)(s),10000))). $ <eq-geo>

其中 $R_j$ 为用于生成该样本观测结果的有效接收半径。式 @eq-geo 中，交会角越接近直角，得分越大；第二检测点距离可能位置过远时，该样本不计入有效接收；分母中的距离项用于抑制远距离观测带来的误差放大。该式只用于比较候选点，最终定位区域仍按式 @eq-update 的硬约束更新。

因此，第二检测点取为

$ s^*=arg max_(s in C) U_"geo"(s). $ <eq-geo-opt>

计算时采用逐级网格搜索：先以 250 m 的间距遍历候选区域，再在得分最高的若干位置附近用 100 m 和 50 m 网格细化。每一层只保留得分最高的少量候选点，最后取几何得分最大的点作为第二检测点。候选域、网格和接收条件在各对照策略中保持一致。

#algorithm[
  算法 1　第二检测点选择
][
  输入：第一次检测点 $S_1$、示向度 $theta_1$、候选区域 $C$、目标位置数 $N_G$ 和误差节点数 $N_e$。\

  Step 1：根据式 @eq-w 和式 @eq-f1 构造第一次定位区域 $F_1$；\

  Step 2：在 $F_1$ 内选取可能的干扰源位置，并在区域 $C$ 内生成第二检测点候选点；\

  Step 3：对每个候选点计算第二次检测的各种可能结果，并按式 @eq-update 求得 $F_2$；\

  Step 4：按式 @eq-geo 计算每个候选点的几何得分；\

  Step 5：按照 250→100→50 m 的顺序逐级细化，取几何得分最大的点作为第二检测点。
]

 #figure(rect(width:78%, height:22%, stroke:0.7pt + luma(60%))[#align(center)[*图片占位符* \\ 建议内容：不同第二检测点选择方法得到的定位区域直径分布]], caption: [不同第二检测点选择方法的定位区域直径比较])


#sec[6.5 不同选点方法的比较]

为检验几何选点方法的效果，设置几何选点法、平均 GDOP 法、FIM E-最优法和随机选点法四种方法。几何选点法按照式 @eq-geo 选择第二检测点；平均 GDOP 法按照局部几何精度因子选点；FIM E-最优法按照 Fisher 信息矩阵的最小特征值选点；随机选点法则在相同候选区域内随机选择检测点。

除第二检测点的选择方法外，四种方法均使用相同的第一次检测结果、测试场景、示向误差和定位区域计算方法。实验生成 400 个基础状态，每个状态进行 25 次第二次检测，共得到 10000 个测试场景，随机种子为 20260911。

主要比较第二次检测后的定位区域直径，结果如表 6-1 所示。

#table-title[
  表 6-1　四种第二检测点选择方法的定位区域直径（m）
]

#table(
  columns: (1.65fr, 1fr, 1fr, 1fr, 1fr, 1fr),
  align: center,
  stroke: 0.35pt,

  [方法],
  [均值],
  [中位数],
  [P90],
  [P95],
  [最大值],

  [平均 GDOP],
  [52.38],
  [47.54],
  [71.66],
  [81.12],
  [306.71],

  [几何选点],
  [67.67],
  [56.27],
  [120.21],
  [142.46],
  [453.83],

  [FIM E-最优],
  [193.41],
  [66.42],
  [609.44],
  [817.15],
  [1352.20],

  [随机选点],
  [588.04],
  [278.29],
  [1495.00],
  [1495.00],
  [1495.00],
)


#sec[6.6 数值结果分析]

由表 6-1 可见，几何选点法的定位区域直径均值为 67.67 m，中位数为 56.27 m；平均 GDOP 法的均值为 52.38 m，FIM E-最优法和随机选点法的均值分别为 193.41 m 和 588.04 m。几何选点法的优势在于计算过程只使用交会角、距离和接收条件，选点耗时较短，适合作为后续多干扰源搜索中的快速候选生成方法。

几何选点法的 P90 和 P95 分别为 120.21 m 和 142.46 m，说明在两条示向线接近平行或第二次观测未形成有效交会时，定位区域仍可能较大。因此，实际使用时应在得到第二次观测后重新计算硬定位区域；若区域仍过大，则继续增加检测点，而不能仅依据几何得分宣称已经完成精确定位。

平均 GDOP 法在本组实验中的平均直径较小，但它依赖局部高斯误差近似，不能代替 $plus.minus 1^"°"$ 硬边界。FIM E-最优法虽然使用的信息矩阵条件较强，但在部分近平行情形下出现较大的定位尾部；随机选点的结果最不稳定。

 #figure(rect(width:78%, height:22%, stroke:0.7pt + luma(60%))[#align(center)[*图片占位符* \\ 建议内容：四种第二检测点选择方法的定位区域直径经验分布]], caption: [四种第二检测点选择方法的定位区域直径经验分布])


#sec[6.7 结论]

第一次检测后，可根据示向度的 $plus.minus 1 degree$ 误差范围和目标圆域确定干扰源的定位区域 $F_1$。第二检测点先在区域 $C$ 内生成，再以交会角、两段距离和接收条件构成的几何得分选择位置。

实验表明，几何选点法计算速度快，适合在候选点较多时快速确定第二检测点；平均 GDOP 法在本组实验中的定位直径较小，但依赖局部误差近似。无论采用哪种选点方法，第二次观测后都必须按硬约束重新计算 $F_2$，并在定位区域仍较大时继续检测。上述结论只适用于本文的候选域、采样设置和误差边界，不能推出几何选点在所有场景下均为最优。
#major[七、问题三：全向干扰源的自动搜索、定位与清除]
#sec[7.1 模型建立与任务分解]
问题三中所有干扰源均为全向源。机器狗从原点出发，在半径 $1800"m"$ 的目标圆域内，对 $20$ 个频道逐一检测。设频道 $c$ 的状态为 $q_c in {"UNKNOWN","FOUND","CLEARED","ABSENT"}$，并以 $U_c$ 表示尚未排除的可能位置集合、以 $F_c$ 表示已发现后的硬可行集。策略目标是在满足“所有真实源均被清除”的条件下最小化虚拟时间。

即时动作代价定义为
$J(a|s) = t_"move" + t_"switch" + t_"detect" + t_"action"
  + hat(T)_"rem" + lambda R_"tail".$

其中前四项是动作本身耗时，$hat(T)_"rem"$ 为预计剩余路线时间，$R_"tail"$ 为尾部风险项。路线排序只影响访问顺序，不改变硬可行集及清除证书。

#figure(rect(width:84%,height:24%,stroke:0.7pt+luma(60%))[#align(center+horizon)[*图7  问题三两层策略结构（待绘制）*\\确定性保证层：覆盖、不存在证明、MEC 清除证书；\\效率优化层：顺路观测、开放路线和滚动重规划。]], caption:[问题三的模型结构。]) <fig-q3-model>

#sec[7.2 七点覆盖与不存在证明]
取原点及半径 $1200"m"$ 的正六边形六顶点作为覆盖点，记
$V_k=1200(cos theta_k,sin theta_k), quad theta_k=theta_0+(k-1)60 degree.$
七点集合记为 $"mathcal" V={O,V_1,dots,V_6}$。计算得到目标圆域内任一点到最近覆盖点的最坏距离为 $968.902"m"<1000"m"$。因此某频道在七点均返回 `no_signal` 时，可以严格证明该频道不存在源；不能以单个检测点的无信号结果替代该证明。

#figure(rect(width:84%,height:23%,stroke:0.7pt+luma(60%))[#align(center+horizon)[*图8  七点覆盖布局（待绘制）*\\标出 $O$、$V_1$--$V_6$、搜索圆及 $968.902"m"$ 最坏覆盖半径。]], caption:[七点覆盖及不存在证书。]) <fig-q3-cover>

#sec[7.3 硬可行集与安全清除]
在位置 $p$ 测得示向度 $hat phi$ 时，采用角误差带、接收半径和目标圆域构造外包
$F_c^+ = F_c inter B(O, 1800) inter B(p, 1500)
  inter { x : op("ang")(x - p) in [hat(phi) - 1 degree, hat(phi) + 1 degree] }
  inter { x : norm(x - p) > 5 }.$

实现用边长 $5"m"$ 的闭方格保守外包。对所有保留方格角点求最小外接圆 $"MEC"(P_c)=(m_c,r_c)$，当 $r_c<=19.75"m"$ 时，在 $m_c$ 处执行 `/clear` 必然成功。若长期不能达到该阈值，则访问方格中心构成有限覆盖。

#sec[7.4 路线与算法]
覆盖点按 $V_1 -> V_2 -> dots.h -> V_6$ 顺序推进；在相邻覆盖点之间允许插入已经进入角窗口的定位或清除任务。每次执行一个首节点后，依据真实位置和新观测重新规划，以避免局部误差累积。

#algorithm("算法 3  全向源滚动搜索与清除", [*输入：* 20 个频道、七点覆盖集合及题面物理参数。\\*Step 1.* 在 $O$ 点扫描 UNKNOWN 频道，建立 $U_c$ 与状态。\\*Step 2.* 依次访问 $V_1$--$V_6$，对方向观测执行硬集合更新。\\*Step 3.* 对 FOUND 频道优先执行 MEC 清除；不能认证时生成有限覆盖圆心。\\*Step 4.* 以 $J(a|s)$ 选择下一动作，每次只执行首节点并立即重规划。\\*Step 5.* 七点完成且所有 UNKNOWN 频道均获不存在证书、所有 FOUND 频道均已清除时调用 `/exit`。])

#sec[7.5 演练统计与正式测试]
演练中记录总清除数 $N_"clear"$、真实源总数 $N$、各动作耗时及总虚拟时间 $T$，计算
$rho = frac(N_"clear", N), quad bar(T) = frac(T, N_"clear").$

正式测试结果按题面表格填写；案例编码、清除数、平均定位清除时间和程序运行时间在测试完成后补入。
#table-title[表4  问题三正式测试结果]
#table(columns:(1.6fr,0.8fr,1fr,1fr), align:center, stroke:0.35pt,[测试案例编码],[清除干扰源个数],[平均定位清除时间],[程序运行时间],[测试1：待补入],[],[],[],[测试2：待补入],[],[],[],[测试3：待补入],[],[],[])
#figure(rect(width:84%,height:22%,stroke:0.7pt+luma(60%))[#align(center+horizon)[*图9  问题三正式测试路线图（待绘制）*\\叠加覆盖点、检测点、定位点、清除点和源编号。]], caption:[问题三正式测试路线占位图。]) <fig-q3-official>

#major[八、问题四：全向与定向干扰源混合场景]
#sec[8.1 混合源的困难与基本原则]
问题四中干扰源可能为全向源，也可能为定向源，定向方向及其个数均未知。此时 `no_signal` 只表示当前位置未接收到信号，可能由距离过远或检测点不在定向源的 $180 degree$ 覆盖扇区内造成。因此，问题三的“七点无信号即可判定不存在”不能直接沿用。本文保留七点路线作为基础骨架，但将不存在判据改为“全覆盖验证加定向方向排除”。

对频道 $c$ 维护类型集合 $T_c in {"omni", "directional"}$、方向可行集合
$A_c subset.eq [0, 360 degree)$ 和位置外包 $F_c$。初始时
$A_c = [0, 360 degree)$；每次无信号观测仅更新方向和距离约束，不删除未被必然覆盖的位置。


#figure(rect(width:84%,height:24%,stroke:0.7pt+luma(60%))[#align(center+horizon)[*图10  定向源观测几何（待绘制）*\\显示源位置、定向方向、$180 degree$ 覆盖扇区、检测点及“无信号但不可排除”的情形。]], caption:[定向源的无信号辨识示意图。]) <fig-q4-geometry>

#sec[8.2 定向方向的可行集合更新]
设源位置为 $x$，定向方向为 $alpha$，检测点为 $p$。若检测到 `direction`，则位置满足
$op("ang")(x - p) in [hat(phi) - 1 degree, hat(phi) + 1 degree],$

且必有
$op("ang")(x - p) - alpha in [-90 degree, 90 degree]$。
若返回 `no_signal`，则只能断言以下析取条件成立：

$||x - p|| > R_c quad or quad
  |op("wrap")(op("ang")(x - p) - alpha)| > 90 degree.$

因此更新应写成

$(F_c, A_c)^+ = (F_c, A_c) inter
  { (x, alpha) :
    ||x - p|| > R_c or
    |op("wrap")(op("ang")(x - p) - alpha)| > 90 degree
  },$

再对 $(x, alpha)$ 投影得到新的位置外包。该联合集合保留了“距离原因”和“方向原因”两种可能，避免误删真值。


#sec[8.3 双侧验证与源类型判别]
为区分两类源，在已有候选位置附近选择两个方位差尽量接近 $180 degree$ 的检测点，且二者到候选区中心距离均不超过 $1500"m"$。若两个相反侧检测点均稳定返回示向度，则可将频道标记为全向候选；若一侧有信号而另一侧无信号，则保留定向候选并收缩 $A_c$。对仍无法判别的频道，继续在第三个非共线方向检测，直到方向可行集合的角宽小于阈值或位置已满足清除证书。

#figure(rect(width:84%,height:23%,stroke:0.7pt+luma(60%))[#align(center+horizon)[*图11  双侧验证策略（待绘制）*\\两处相反侧检测点、方向扇区及候选位置交集。]], caption:[双侧检测对源类型的判别。]) <fig-q4-two-sided>

#sec[8.4 混合场景下的清除证书]
定向源一旦进入 $20"m"$ 清除半径，光学定位与激光清除与源方向无关。因此安全清除仍由位置外包决定：若
$op("MEC")(P_c) = (m_c, r_c)$
且
$r_c <= 19.75"m"$，
则在 $m_c$ 处清除。若仅得到联合集合 $(F_c, A_c)$ 而位置投影仍较大，则忽略方向变量对清除的影响，继续收缩位置外包；不能因方向已确定而直接清除。若无法形成单一 MEC 证书，则用保留方格中心的有限 $20"m"$ 覆盖逐点清除。


#sec[8.5 搜索路线与终止条件]
七点仍按固定顺序访问，但覆盖终止条件改为：每个频道均满足“已清除”或“已获得全覆盖下的不存在证书”。对未知频道，优先安排能最大化候选位置收缩且覆盖方向空间的检测点；对 FOUND 频道，优先安排双侧验证和安全清除。候选动作代价仍取
$J_4(a | s) =
  t_"move" +
  t_"switch" +
  t_"detect" +
  t_"action" +
  hat(T)_"rem" +
  lambda R_"type",$

其中 $R_"type"$ 对方向类型不确定且位置外包较大的频道给予更高权重。

#algorithm(
  "算法 4  混合源搜索、判别与清除",
  [
    *输入：* 20 个频道、七点覆盖骨架和物理参数。\
    *Step 1.* 在 $O$ 点逐频道检测；对 `direction` 建立 $(F_c, A_c)$，对 `no_signal` 仅做析取约束更新。\
    *Step 2.* 沿 $V_1$--$V_6$ 推进；对疑似 `FOUND` 频道安排相反侧检测和非共线复测。\
    *Step 3.* 若位置外包满足 MEC 证书，则执行 `/clear`；否则访问有限覆盖圆心。\
    *Step 4.* 对每个频道维护“未判别、全向候选、定向候选、已清除、已排除”状态，显著更新后重新排序。\
    *Step 5.* 只有所有频道均有清除证书或严格不存在证书时才调用 `/exit`。
  ]
)


#sec[8.6 正确性、复杂度与验证设计]
只要联合硬集合始终包含真实的 $(x,alpha)$，任何观测更新都不会删除真值；清除只在位置投影的 MEC 或有限覆盖证书成立时执行，因此方向未知不会破坏清除安全性。设频道方格数为 $N_c$、候选节点数为 $M$，一次联合更新为 $O(N_c K)$，其中 $K$ 为方向区间分段数；开放路线动态规划仍为 $O(M^2 2^M)$。

演练阶段应分别统计清除率、平均定位清除时间、全向/定向分类混淆次数、误清除次数和未判别频道数。正式测试结果沿用问题三表格格式，案例编码和时间数据待完成测试后补入。
#table-title[表5  问题四正式测试结果]
#table(columns:(1.6fr,0.8fr,1fr,1fr), align:center, stroke:0.35pt,[测试案例编码],[清除干扰源个数],[平均定位清除时间],[程序运行时间],[测试1：待补入],[],[],[],[测试2：待补入],[],[],[],[测试3：待补入],[],[],[])
#figure(rect(width:84%,height:23%,stroke:0.7pt+luma(60%))[#align(center+horizon)[*图12  问题四正式测试路线与方向扇区（待绘制）*\\区分全向源、定向源、检测点、候选位置和最终清除点。]], caption:[问题四正式测试路线图占位图。]) <fig-q4-official>

#sec[8.7 问题四结论]
问题四的核心不是把问题三的无信号规则原样复用，而是在位置变量之外引入定向方向变量，并对无信号采用析取约束。七点路线提供基础搜索骨架，双侧与非共线观测负责方向辨识，MEC 或有限圆盘覆盖负责最终清除。由此，搜索效率可以通过路线排序改善，而正确性始终由联合硬集合和清除证书保证。

